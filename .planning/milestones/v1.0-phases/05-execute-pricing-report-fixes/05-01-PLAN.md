---
phase: 05-execute-pricing-report-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/usePricingFilters.ts
  - utils/pricingGrouping.ts
  - hooks/usePricingExport.ts
autonomous: true
requirements:
  - PRF-01
  - PRF-02
  - PRF-04
  - PRF-05
  - PRF-06
gap_closure: true
must_haves:
  truths:
    - "Three uncommitted files (usePricingFilters.ts, pricingGrouping.ts, usePricingExport.ts) are committed to git with no further modifications"
    - "filterDoorGroups in pricingGrouping.ts filters per-door within each group and recomputes totalQty/totalPrice/materials/floors/buildings/prep for the new group"
    - "filterDoorGroups returns groups unchanged when all four filter arrays are empty (noFilters branch)"
    - "usePricingFilters.ts uses g.totalQty (not g.doors.length) for totalDoorCount, totalFrameCount, and totalHwCount"
    - "usePricingFilters.ts replaces filterHardwareGroups with the filteredDoorsForHw + groupHardwareItems re-grouping approach"
    - "usePricingExport.ts uses the Phase 1 PDF theme (buildAutoTableOptions, addPageNumbers, DEFAULT_THEME, BRAND_NAVY, ROW_ALT_FILL)"
    - "PricingReportConfig.tsx already passes visibleDoors/visibleFrames to usePricingExport (verified, no edit needed) — PRF-06 story closed"
    - "Project builds via `npm run build` without new TypeScript errors in the three committed files"
  artifacts:
    - path: ".planning/phases/05-execute-pricing-report-fixes/05-01-SUMMARY.md"
      provides: "Audit log of pre-commit grep verifications, git diff summary, commit hash, post-commit build result"
      contains: "g.totalQty"
  key_links:
    - from: "hooks/usePricingFilters.ts"
      to: "utils/pricingGrouping.ts (filterDoorGroups, groupHardwareItems)"
      via: "import { filterDoorGroups, groupHardwareItems } from '@/utils/pricingGrouping'"
      pattern: "filterDoorGroups|groupHardwareItems"
    - from: "components/pricing/PricingReportConfig.tsx (already committed)"
      to: "hooks/usePricingExport.ts"
      via: "doorGroups: visibleDoors / frameGroups: visibleFrames in the usePricingExport call"
      pattern: "doorGroups: visibleDoors"
---

<objective>
Commit the three uncommitted files that already contain the correct level-filter, per-door filtering, and qty-count fixes. This closes Phase 3 plans 03-03 and 03-04 by formalizing work that is already correctly implemented in the working tree, plus the deeper per-door filterDoorGroups rewrite (beyond what 03-04 specified). No code changes are required — only verification of the working-tree state and an atomic commit.

Purpose: Establish a clean, committed baseline so Plan 05-02 (data source fix) can build on top without conflict, and so verification (Plan 05-03) sees the intended code state.

Output: One git commit containing exactly three files; one SUMMARY.md documenting the pre/post-commit state and grep evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-execute-pricing-report-fixes/05-RESEARCH.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-03-PLAN.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-04-PLAN.md

<interfaces>
<!-- Expected working-tree state (already verified by 05-RESEARCH.md): -->
<!-- 1. hooks/usePricingFilters.ts lines 239-241: -->
<!--    const totalDoorCount  = useMemo(() => visibleDoors.reduce((s, g) => s + g.totalQty, 0),    [visibleDoors]); -->
<!--    const totalFrameCount = useMemo(() => visibleFrames.reduce((s, g) => s + g.totalQty, 0),   [visibleFrames]); -->
<!--    const totalHwCount    = useMemo(() => visibleHardware.reduce((s, g) => s + g.totalQty, 0), [visibleHardware]); -->
<!-- 2. hooks/usePricingFilters.ts: removed `filterHardwareGroups` from import; introduced filteredDoorsForHw memo and groupHardwareItems re-grouping. -->
<!-- 3. utils/pricingGrouping.ts filterDoorGroups (lines 354+): noFilters short-circuit + per-door filter loop that recomputes totalQty/totalPrice/materials/floors/buildings/prep. -->
<!-- 4. hooks/usePricingExport.ts: handleDownloadPdf rewritten to use Phase 1 theme primitives (buildAutoTableOptions, addPageNumbers, loadLogoDataUrl, DEFAULT_THEME, PDF_MARGIN, HEADER_BAR_HEIGHT, BRAND_NAVY, ROW_ALT_FILL); proposal/expenses/tax tables aligned to BRAND_NAVY + alternateRowStyles + repeatHeaders + rowPageBreak:'avoid'. -->

<!-- Already-committed reference points (DO NOT MODIFY): -->
<!-- - utils/xlsxParser.ts line 73 contains: 'buildingn area', 'building area', 'buildingarea' (Bug A fixed) -->
<!-- - utils/hardwareTransformers.ts line 169 transformDoors() contains: `?? row.buildingLocation ?? row.buildingArea` (Bug B fixed) -->
<!-- - components/pricing/PricingReportConfig.tsx lines 245-246 contain: `doorGroups: visibleDoors,` and `frameGroups: visibleFrames,` (Bug C fixed) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify uncommitted working tree matches research findings and commit atomically</name>
  <files>hooks/usePricingFilters.ts, utils/pricingGrouping.ts, hooks/usePricingExport.ts</files>
  <read_first>
    - .planning/phases/05-execute-pricing-report-fixes/05-RESEARCH.md (sections: "Uncommitted Changes Audit" lines 39-113, "Current State of Each Target File" lines 116-170, "Plan 05-01 commit verification commands" lines 519-528)
    - hooks/usePricingFilters.ts (full file — confirm lines 239-241 use g.totalQty; confirm filterHardwareGroups is no longer imported; confirm filteredDoorsForHw + visibleHardware re-grouping memos exist)
    - utils/pricingGrouping.ts (full file — confirm filterDoorGroups starting at line ~354 has the noFilters short-circuit and the per-door filter loop)
    - hooks/usePricingExport.ts (full file — confirm Phase 1 theme imports and usage)
    - utils/xlsxParser.ts (line 73 — confirm 'buildingn area' / 'building area' / 'buildingarea' aliases are present in the committed file; this is a no-touch reference)
    - utils/hardwareTransformers.ts (line 169 — confirm `?? row.buildingArea` is present in transformDoors() in the committed file; this is a no-touch reference)
    - components/pricing/PricingReportConfig.tsx (lines 241-271 — confirm `doorGroups: visibleDoors` and `frameGroups: visibleFrames` are present in the committed file; this is a no-touch reference)
  </read_first>
  <action>
Execute these steps in order. Do NOT edit any source file in this plan. The working tree already contains the correct state; this task only verifies and commits.

**Step 1 — Verify already-committed reference points (no edits should be needed):**

```bash
# Bug A — xlsxParser aliases (already committed)
grep -c "'buildingn area'" utils/xlsxParser.ts
# Expected output: 1

grep -c "'building area'" utils/xlsxParser.ts
# Expected output: 1

grep -c "'buildingarea'" utils/xlsxParser.ts
# Expected output: 1

# Bug B — hardwareTransformers fallback in transformDoors (already committed)
grep -n "buildingLocation:" utils/hardwareTransformers.ts | head -5
# The line at ~169 should contain: `?? row.buildingLocation ?? row.buildingArea`

# Bug C — PricingReportConfig passes filtered groups (already committed)
grep -c "doorGroups: visibleDoors" components/pricing/PricingReportConfig.tsx
# Expected output: 1

grep -c "frameGroups: visibleFrames" components/pricing/PricingReportConfig.tsx
# Expected output: 1
```

If any of these greps return 0 or a different line, HALT — the research findings are stale and this plan must be re-evaluated.

**Step 2 — Verify the working-tree (uncommitted) state of the three files to be committed:**

```bash
# Bug D / Plan 03-04 — totalDoorCount, totalFrameCount, totalHwCount use g.totalQty
grep -n "g.totalQty" hooks/usePricingFilters.ts
# Expected: 3 matches on lines around 239-241 inside .reduce() callbacks

# usePricingFilters: filterHardwareGroups import removed
grep -n "filterHardwareGroups" hooks/usePricingFilters.ts
# Expected: 0 matches (import line should NOT include filterHardwareGroups)

# usePricingFilters: filteredDoorsForHw + re-grouping pattern present
grep -c "filteredDoorsForHw" hooks/usePricingFilters.ts
# Expected: >= 2 (declaration + use in visibleHardware memo)

grep -c "groupHardwareItems(hardwareSets, filteredDoorsForHw)" hooks/usePricingFilters.ts
# Expected: 1

# pricingGrouping: filterDoorGroups rewrite — noFilters short-circuit + per-door loop
grep -c "noFilters" utils/pricingGrouping.ts
# Expected: >= 1

grep -c "matching.length === 0" utils/pricingGrouping.ts
# Expected: 1

grep -c "totalPrice: g.unitPrice \* qty" utils/pricingGrouping.ts
# Expected: 1

# usePricingExport: Phase 1 PDF theme imports/usage
grep -c "buildAutoTableOptions" hooks/usePricingExport.ts
# Expected: >= 1

grep -c "BRAND_NAVY" hooks/usePricingExport.ts
# Expected: >= 1

grep -c "addPageNumbers" hooks/usePricingExport.ts
# Expected: >= 1
```

If any check fails, HALT — the working tree no longer matches research findings; do NOT commit.

**Step 3 — Capture the diff stats (for audit log):**

```bash
git diff --stat hooks/usePricingFilters.ts utils/pricingGrouping.ts hooks/usePricingExport.ts
```

Save the output text — it goes into the SUMMARY.

**Step 4 — Run build to confirm no new TypeScript errors in the three files:**

```bash
npm run build
```

`tsconfig.json` has `strict: false` and `next.config.ts` has `ignoreBuildErrors: true`, so the build will succeed even with pre-existing errors. The check is: scan the build output and look for NEW errors that reference `hooks/usePricingFilters.ts`, `utils/pricingGrouping.ts`, or `hooks/usePricingExport.ts`. If a new error appears in any of those three files, HALT and do not commit.

**Step 5 — Stage the three files (and ONLY these three files):**

```bash
git add hooks/usePricingFilters.ts utils/pricingGrouping.ts hooks/usePricingExport.ts
```

Do NOT use `git add -A` or `git add .` — other untracked changes (e.g., `.claude/settings.local.json`) must NOT be in this commit.

Verify staging contains exactly three files:

```bash
git diff --cached --name-only
# Expected output, exactly three lines:
# hooks/usePricingExport.ts
# hooks/usePricingFilters.ts
# utils/pricingGrouping.ts
```

If the list contains any other file, run `git restore --staged <unwanted-file>` until exactly the three files remain.

**Step 6 — Create the commit:**

Use a heredoc to keep the message intact:

```bash
git commit -m "$(cat <<'EOF'
feat(05-01): commit pricing filter rewrite — per-door filtering + qty-based counts + PDF theme

Closes PRF-02, PRF-04, PRF-05, PRF-06 (verification step) and the
implementation half of PRF-01 (xlsxParser alias + hardwareTransformers
fallback are already in committed code).

- hooks/usePricingFilters.ts: replace filterHardwareGroups with
  filteredDoorsForHw + groupHardwareItems re-grouping; use g.totalQty
  (not g.doors.length) for totalDoorCount / totalFrameCount / totalHwCount.
- utils/pricingGrouping.ts: filterDoorGroups rewrite — per-door filter
  inside each group with recomputed totalQty/totalPrice/materials/
  floors/buildings/prep; short-circuit when no filters active.
- hooks/usePricingExport.ts: align PDF export with Phase 1 theme
  (buildAutoTableOptions, addPageNumbers, DEFAULT_THEME, BRAND_NAVY,
  ROW_ALT_FILL); section-per-page; proposal/expenses/tax tables aligned.

Phase 5 plan: gsd plan-phase 05 --gaps.
Plan: 05-01-PLAN.md
EOF
)"
```

**Step 7 — Capture the commit hash for the SUMMARY:**

```bash
git log -1 --format="%H %s"
```

Save this for the SUMMARY.md.

**Step 8 — Post-commit verification:**

```bash
# Confirm the three files are no longer in working-tree diff:
git diff --name-only hooks/usePricingFilters.ts utils/pricingGrouping.ts hooks/usePricingExport.ts
# Expected output: empty (no modifications outstanding)

# Confirm the grep checks from Step 2 still pass against the committed file (post-commit, same content):
grep -c "g.totalQty" hooks/usePricingFilters.ts
# Expected: 3
```

**Step 9 — Write SUMMARY.md:**

Create `.planning/phases/05-execute-pricing-report-fixes/05-01-SUMMARY.md` with the following content (filling in actual values from the commands above):

```markdown
<!-- summary-frontmatter-guard -->
phase: 05-execute-pricing-report-fixes
plan: 01
status: complete
requirements-completed: [PRF-02, PRF-04, PRF-05, PRF-06]
requirements-partial: [PRF-01]
commit: <commit-hash from Step 7>
<!-- summary-frontmatter-guard -->

# 05-01 SUMMARY: Commit pricing filter rewrite

## What was done
Committed three uncommitted files atomically. No source edits — only verification
and commit:
- hooks/usePricingFilters.ts
- utils/pricingGrouping.ts
- hooks/usePricingExport.ts

## Pre-commit grep evidence

### Already-committed reference points (no edits needed)
- `grep -c "'buildingn area'" utils/xlsxParser.ts` → 1
- `grep -c "row.buildingArea" utils/hardwareTransformers.ts` → <captured value>
- `grep -c "doorGroups: visibleDoors" components/pricing/PricingReportConfig.tsx` → 1
- `grep -c "frameGroups: visibleFrames" components/pricing/PricingReportConfig.tsx` → 1

### Working-tree state (committed in this plan)
- `grep -c "g.totalQty" hooks/usePricingFilters.ts` → 3
- `grep -c "filterHardwareGroups" hooks/usePricingFilters.ts` → 0
- `grep -c "filteredDoorsForHw" hooks/usePricingFilters.ts` → <captured value>
- `grep -c "noFilters" utils/pricingGrouping.ts` → <captured value>
- `grep -c "matching.length === 0" utils/pricingGrouping.ts` → 1
- `grep -c "buildAutoTableOptions" hooks/usePricingExport.ts` → <captured value>
- `grep -c "BRAND_NAVY" hooks/usePricingExport.ts` → <captured value>

## Diff stats
<paste output of `git diff --stat ...` from Step 3>

## Build result
npm run build: <PASS / FAIL summary; if PASS, note any pre-existing errors but no new ones in the three files>

## Commit hash
<paste from Step 7>

## Requirement status after this commit
- PRF-01: PARTIAL — xlsxParser aliases + hardwareTransformers transformDoors fallback
  are in committed code (verified by grep). The transformFromFinalJson buildingLocation
  chain at hardwareTransformers.ts line 309 still lacks `?? door.buildingArea` — addressed in 05-02.
- PRF-02: COMPLETE — g.totalQty now used at lines 239-241.
- PRF-04: COMPLETE — filterDoorGroups recomputes totalQty from matching doors.
- PRF-05: COMPLETE — filterDoorGroups recomputes totalPrice = g.unitPrice * qty.
- PRF-06: COMPLETE (implementation) — visibleDoors/visibleFrames already flow
  to usePricingExport from PricingReportConfig (committed). Final verification: Plan 05-03.

## What this plan does NOT close
- PRF-03 (pricing page data source) — Plan 05-02
- PRF-07 / PRF-08 (manual verification) — Plan 05-03
```
  </action>
  <verify>
    <automated>git log -1 --name-only --format="" | sort | tr -d '\r' | xargs -I{} echo {} | grep -E "^(hooks/usePricingExport.ts|hooks/usePricingFilters.ts|utils/pricingGrouping.ts)$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The most recent git commit (HEAD) contains exactly three files: `hooks/usePricingExport.ts`, `hooks/usePricingFilters.ts`, `utils/pricingGrouping.ts` (verifiable: `git log -1 --name-only --format=""` lists those three lines and nothing else)
    - Commit message starts with `feat(05-01):` (verifiable: `git log -1 --format=%s` matches `^feat\(05-01\):`)
    - `git diff --name-only hooks/usePricingFilters.ts utils/pricingGrouping.ts hooks/usePricingExport.ts` returns empty (no outstanding working-tree changes in the three files)
    - `grep -c "g.totalQty" hooks/usePricingFilters.ts` returns 3 after the commit
    - `grep -c "filterHardwareGroups" hooks/usePricingFilters.ts` returns 0 after the commit
    - `grep -c "noFilters" utils/pricingGrouping.ts` returns >= 1 after the commit
    - `grep -c "buildAutoTableOptions" hooks/usePricingExport.ts` returns >= 1 after the commit
    - `grep -c "'buildingn area'" utils/xlsxParser.ts` returns 1 (already-committed reference state preserved)
    - `grep -c "doorGroups: visibleDoors" components/pricing/PricingReportConfig.tsx` returns 1 (already-committed reference state preserved)
    - `.planning/phases/05-execute-pricing-report-fixes/05-01-SUMMARY.md` exists and contains the literal strings `g.totalQty`, `commit:`, `PRF-02`, `PRF-04`, `PRF-05`, `PRF-06` (verifiable: `grep` each one)
    - `npm run build` produces no new TypeScript errors mentioning `hooks/usePricingFilters.ts`, `utils/pricingGrouping.ts`, or `hooks/usePricingExport.ts`
  </acceptance_criteria>
  <done>
    Three target files committed atomically with the documented message. SUMMARY.md written with all required keywords and the commit hash. No outstanding working-tree changes in the three committed files. Build does not introduce new errors in the committed files.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:
1. `git log -1 --name-only --format=""` lists exactly the three target files (no more, no less).
2. The committed code passes all pre-commit grep checks documented in Step 2 of the action.
3. SUMMARY.md exists and documents the commit hash, grep evidence, and per-requirement status.
4. The "already-committed" reference points (xlsxParser, hardwareTransformers transformDoors fallback, PricingReportConfig export calls) remain untouched and still grep-positive.
</verification>

<success_criteria>
- PRF-02, PRF-04, PRF-05 fully closed (implementation level).
- PRF-06 implementation closed (UI verification deferred to 05-03).
- PRF-01 partially closed (implementation half: xlsxParser + transformDoors fallback already in committed code; the transformFromFinalJson fallback for the finalJson data path is handled in 05-02).
- Working tree clean for the three target files; baseline is ready for Plan 05-02.
- 05-01-SUMMARY.md exists with grep evidence, commit hash, build result, and requirement status.
</success_criteria>

<output>
After completion, create `.planning/phases/05-execute-pricing-report-fixes/05-01-SUMMARY.md`
</output>

---
phase: 06-wire-export-and-realtime-error-handling
plan: "06.1-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/usePricingExport.ts
  - components/pricing/PricingReportConfig.tsx
  - constants/errors/realtime.ts
  - constants/errors/index.ts
  - hooks/useProjectRealtime.ts
  - hooks/useProjectData.ts
  - hooks/useProjectPersistence.ts
  - views/ProjectView.tsx
autonomous: true
gap_closure: true
requirements:
  - ERR-02
  - ERR-03
  - ERR-06

must_haves:
  truths:
    - "constants/errors/realtime.ts exists in the working tree with REALTIME_ERRORS.SUBSCRIPTION_FAILED"
    - "hooks/usePricingExport.ts has addToast param and all three handlers wrapped in try/catch with PDF_ERRORS.EXPORT_FAILED"
    - "hooks/useProjectRealtime.ts has onError optional callback wired from subscribe err branch"
    - "hooks/useProjectData.ts passes onError to useProjectRealtime with REALTIME_ERRORS.SUBSCRIPTION_FAILED toast"
    - "hooks/useProjectPersistence.ts replaces console.warn with console.error + GENERAL_ERRORS.SAVE_FAILED toast"
    - "views/ProjectView.tsx passes addToast into useProjectPersistence call"
    - "All 7 cherry-picked commits are on AP-Sprint-1 (git log shows 94d5797, 229f5a4, f68f12d, c646aa3, 524d4e8, 1a2a133, 142162f)"
  artifacts:
    - path: "constants/errors/realtime.ts"
      provides: "REALTIME_ERRORS registry with SUBSCRIPTION_FAILED entry"
      exports: ["REALTIME_ERRORS"]
    - path: "hooks/usePricingExport.ts"
      provides: "Try/catch-wrapped export handlers with PDF_ERRORS.EXPORT_FAILED toast"
      contains: "PDF_ERRORS.EXPORT_FAILED"
    - path: "hooks/useProjectRealtime.ts"
      provides: "onError callback ref invoked from subscribe err branch"
      contains: "onErrorRef"
    - path: "hooks/useProjectPersistence.ts"
      provides: "SAVE_FAILED toast in both catch blocks"
      contains: "GENERAL_ERRORS.SAVE_FAILED"
  key_links:
    - from: "hooks/usePricingExport.ts catch blocks"
      to: "addToast"
      via: "PDF_ERRORS.EXPORT_FAILED"
      pattern: "PDF_ERRORS\\.EXPORT_FAILED"
    - from: "hooks/useProjectRealtime.ts subscribe err branch"
      to: "onErrorRef.current"
      via: "onErrorRef.current?.(err as Error)"
      pattern: "onErrorRef\\.current\\?\\."
    - from: "hooks/useProjectPersistence.ts catch blocks"
      to: "addToast"
      via: "GENERAL_ERRORS.SAVE_FAILED"
      pattern: "GENERAL_ERRORS\\.SAVE_FAILED"
---

<objective>
Cherry-pick the 7 clean Phase 6 commits onto AP-Sprint-1 in dependency order, landing all Phase 6 work except the flawed pricing-page commit (77f7e65 — which has an ERR-02 violation and drops useProjectData). The pricing page fix is handled separately in Plan 06.1-02.

Purpose: All Phase 6 implementation exists only on isolated worktree branches. AP-Sprint-1 is still at ecfd9e0 (Phase 5). Every Phase 6 file in the working tree is pre-Phase-6. Cherry-picking the 7 clean commits brings the working tree up to date for all files except app/project/[id]/reports/pricing/page.tsx.

Output: 7 new commits on AP-Sprint-1 covering usePricingExport, PricingReportConfig, realtime error registry, useProjectRealtime, useProjectData, useProjectPersistence, ProjectView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wire-export-and-realtime-error-handling/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick 7 clean Phase 6 commits onto AP-Sprint-1</name>

  <read_first>
    - Run: git log --oneline AP-Sprint-1 | head -5  (confirm HEAD is ecfd9e0 before touching anything)
    - Run: git status  (confirm working tree is clean — no uncommitted changes)
    - Run: git log --oneline worktree-agent-a0103b91f4af5c16f | head -5  (confirm 229f5a4, 94d5797 present)
    - Run: git log --oneline worktree-agent-a9708b695ee55d2eb | head -6  (confirm f68f12d, c646aa3, 524d4e8, 77f7e65 present; note 77f7e65 is NOT cherry-picked)
    - Run: git log --oneline worktree-agent-a8fad0b641bacba68 | head -5  (confirm 1a2a133, 142162f present)
  </read_first>

  <files>
    hooks/usePricingExport.ts
    components/pricing/PricingReportConfig.tsx
    constants/errors/realtime.ts
    constants/errors/index.ts
    hooks/useProjectRealtime.ts
    hooks/useProjectData.ts
    hooks/useProjectPersistence.ts
    views/ProjectView.tsx
  </files>

  <action>
    Cherry-pick exactly 7 commits in the order below. Do NOT cherry-pick 77f7e65 (pricing page with ERR-02 violation).

    Run each git cherry-pick command individually and confirm it succeeds before proceeding to the next. If a conflict occurs, resolve it by keeping the incoming changes for the affected file and then run `git cherry-pick --continue`.

    ORDER (oldest ancestor first within each branch, then branch order: 01 → 02 → 03):

    Step 1 — Plan 06-01 commits (branch: worktree-agent-a0103b91f4af5c16f):
      git cherry-pick 94d5797
      git cherry-pick 229f5a4

    Step 2 — Plan 06-02 commits EXCLUDING 77f7e65 (branch: worktree-agent-a9708b695ee55d2eb):
      git cherry-pick f68f12d
      git cherry-pick c646aa3
      git cherry-pick 524d4e8
      -- STOP HERE. Do NOT cherry-pick 77f7e65. --

    Step 3 — Plan 06-03 commits (branch: worktree-agent-a8fad0b641bacba68):
      git cherry-pick 1a2a133
      git cherry-pick 142162f

    Expected result: AP-Sprint-1 advances from ecfd9e0 by exactly 7 commits. The pricing page (app/project/[id]/reports/pricing/page.tsx) remains unchanged from the AP-Sprint-1 version (the Phase 5 version with useProjectData + noopToast — this is intentional; Plan 06.1-02 will fix it).

    If any cherry-pick produces a conflict on a file OTHER than app/project/[id]/reports/pricing/page.tsx, examine the conflict carefully and accept the incoming change (the Phase 6 version from the worktree branch) unless the conflict is clearly a meaningful divergence from AP-Sprint-1 that should be preserved — in that case, manually merge both sides and preserve all changes.

    If cherry-pick 524d4e8 conflicts on hooks/useProjectData.ts because of the onError addition: the AP-Sprint-1 version of useProjectData.ts does not have onError; the incoming change adds it. Accept the incoming change in full.
  </action>

  <verify>
    <automated>
      # Verify all 7 commits are on AP-Sprint-1
      git log --oneline AP-Sprint-1 | head -10

      # Expected: the 7 SHAs appear in log (cherry-pick creates new SHAs but commit messages match)
      git log --oneline AP-Sprint-1 | head -8 | grep -E "06-01|06-02|06-03"

      # Confirm realtime.ts was created
      test -f constants/errors/realtime.ts && echo "PASS: realtime.ts exists" || echo "FAIL: realtime.ts missing"

      # Confirm PDF_ERRORS in usePricingExport
      grep -c "PDF_ERRORS" hooks/usePricingExport.ts

      # Confirm onError in useProjectRealtime
      grep -c "onErrorRef" hooks/useProjectRealtime.ts

      # Confirm SAVE_FAILED in useProjectPersistence
      grep -c "SAVE_FAILED" hooks/useProjectPersistence.ts

      # Confirm noopToast still in pricing page (AP-Sprint-1 version preserved — Plan 06.1-02 will remove it)
      grep -c "noopToast" "app/project/[id]/reports/pricing/page.tsx"

      # Confirm no hardcoded 'Failed to load pricing report' string landed (77f7e65 was skipped)
      grep -rn "Failed to load pricing report" "app/project/[id]/reports/pricing/page.tsx" || echo "PASS: no hardcoded string"
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log --oneline AP-Sprint-1 | head -10` shows 7 new commits with messages containing "06-01" and "06-02" and "06-03"
    - `constants/errors/realtime.ts` exists and contains `REALTIME_ERRORS` and `RT_SUBSCRIPTION_FAILED`
    - `grep -c "PDF_ERRORS" hooks/usePricingExport.ts` returns a number >= 3 (one per handler)
    - `grep -c "onErrorRef" hooks/useProjectRealtime.ts` returns a number >= 2 (declaration + invocation)
    - `grep -c "SAVE_FAILED" hooks/useProjectPersistence.ts` returns a number >= 2 (one per catch block)
    - `grep -c "noopToast" "app/project/[id]/reports/pricing/page.tsx"` returns 2 (still the Phase 5 version — intentional)
    - `grep "Failed to load pricing report" "app/project/[id]/reports/pricing/page.tsx"` returns no matches (77f7e65 was correctly skipped)
    - `git status` shows working tree clean after all cherry-picks
  </acceptance_criteria>

  <done>All 7 clean Phase 6 commits are on AP-Sprint-1. The pricing page is untouched (still the Phase 5 version with noopToast). No conflicts remain. Working tree is clean.</done>
</task>

</tasks>

<verification>
After the cherry-pick sequence:

1. `git log --oneline AP-Sprint-1 | head -10` — 7 new commits visible, messages match Phase 6 plan work
2. `ls constants/errors/` — shows `realtime.ts` alongside existing files
3. `grep "REALTIME_ERRORS" constants/errors/index.ts` — shows re-export and ERRORS namespace key
4. `grep -c "try {" hooks/usePricingExport.ts` — at least 3 try blocks (one per handler)
5. `grep -c "console.warn" hooks/useProjectPersistence.ts` — returns 0 (all replaced with console.error + toast)
6. `grep "addToast" views/ProjectView.tsx` — shows addToast passed into useProjectPersistence
7. `grep -c "noopToast" "app/project/[id]/reports/pricing/page.tsx"` — returns 2 (AP-Sprint-1 version preserved — expected)
</verification>

<success_criteria>
- All 7 cherry-pick commits land on AP-Sprint-1 without conflicts
- Eight files are updated: usePricingExport.ts, PricingReportConfig.tsx, realtime.ts, errors/index.ts, useProjectRealtime.ts, useProjectData.ts, useProjectPersistence.ts, ProjectView.tsx
- The pricing page (app/project/[id]/reports/pricing/page.tsx) is unchanged from Phase 5 — the noopToast version is preserved for Plan 06.1-02 to reconcile
- No hardcoded error strings from 77f7e65 appear anywhere in the working tree
</success_criteria>

<output>
After completion, create `.planning/phases/06-wire-export-and-realtime-error-handling/06.1-01-SUMMARY.md` following the summary template.
</output>

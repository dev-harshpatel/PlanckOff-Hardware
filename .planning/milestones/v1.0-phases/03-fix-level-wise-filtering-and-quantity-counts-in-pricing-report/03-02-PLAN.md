---
phase: 03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/hardwareTransformers.ts
autonomous: true
requirements:
  - PRF-01
  - PRF-04
  - PRF-05
must_haves:
  truths:
    - "Server/DB load path: when DoorScheduleRow has buildingArea set but no buildingLocation, the resulting Door record has buildingLocation populated from buildingArea"
    - "When DoorScheduleRow.buildingLocation is set, that value still wins (no overwrite)"
    - "When section data (basic_information) has 'BUILDING LOCATION', that value still wins over both row.buildingLocation and row.buildingArea"
  artifacts:
    - path: "utils/hardwareTransformers.ts"
      provides: "buildingLocation fallback chain extended to include row.buildingArea"
      contains: "row.buildingArea"
  key_links:
    - from: "services/doorScheduleService.ts"
      to: "utils/hardwareTransformers.ts"
      via: "DoorScheduleRow.buildingArea (set by mapping 'buildingn area' -> 'buildingArea') flows into transformDoors() and populates Door.buildingLocation as fallback"
      pattern: "row.buildingArea"
---

<objective>
Fix Bug B — `transformDoors()` in `utils/hardwareTransformers.ts` line 169 reads only `row.buildingLocation`, never `row.buildingArea`. The server-side parser (`services/doorScheduleService.ts`) maps the "BUILDINGN AREA" column to `DoorScheduleRow.buildingArea`. Without a fallback, that value is silently dropped and `Door.buildingLocation` is `undefined` for all server/DB-loaded projects that used "BUILDINGN AREA" in source.

Purpose: Enables PRF-01 (level filter) for the server/DB load path. PRF-04 (Total Qty correctness) and PRF-05 (pricing totals) follow because the filter must work for grouping/totals to recompute correctly.
Output: Updated fallback chain in `utils/hardwareTransformers.ts` line 169.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-CONTEXT.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-RESEARCH.md
@utils/hardwareTransformers.ts
@services/doorScheduleService.ts

<interfaces>
<!-- Existing fallback chain in utils/hardwareTransformers.ts line 169. -->
<!-- The DoorScheduleRow type (defined in services/doorScheduleService.ts) has both buildingLocation and buildingArea. -->
<!-- Server-side parser writes buildingArea when source column is 'BUILDINGN AREA'; writes buildingLocation when source is 'BUILDING LOCATION'. -->

Current (line 169):
```typescript
buildingLocation: bi?.['BUILDING LOCATION']   ?? d?.['BUILDING LOCATION']  ?? row.buildingLocation,
```

Resolution priority (??-chain): basic_information section -> door section -> top-level row.buildingLocation. The bug is that row.buildingArea is never consulted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add row.buildingArea as final fallback in buildingLocation assignment</name>
  <files>utils/hardwareTransformers.ts</files>
  <read_first>
    - utils/hardwareTransformers.ts (read lines 128-220 to see the full transformDoors() return object including the buildingLocation line at 169 and the surrounding fields like buildingTag at 168 and operation at 171 — confirm exact alignment style)
    - services/doorScheduleService.ts (read the full file or grep for 'buildingArea' to confirm DoorScheduleRow type has buildingArea field, and confirm the column mapping 'buildingn area' -> 'buildingArea')
    - .planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-RESEARCH.md (Bug B section lines 158-167)
  </read_first>
  <action>
Open `utils/hardwareTransformers.ts`. Locate line 169 which currently reads:

```typescript
      buildingLocation: bi?.['BUILDING LOCATION']   ?? d?.['BUILDING LOCATION']  ?? row.buildingLocation,
```

Replace with:

```typescript
      buildingLocation: bi?.['BUILDING LOCATION']   ?? d?.['BUILDING LOCATION']  ?? row.buildingLocation ?? row.buildingArea,
```

Add `?? row.buildingArea` as the FINAL element of the `??` chain. The order matters:
- `bi?.['BUILDING LOCATION']` — basic_information section value (highest priority)
- `d?.['BUILDING LOCATION']` — door section value
- `row.buildingLocation` — top-level row value (existing fallback)
- `row.buildingArea` — NEW final fallback (only used if all prior are nullish)

This guarantees:
- If a project has BOTH buildingLocation and buildingArea set, buildingLocation wins (no behavior change)
- If a project has ONLY buildingArea (Mixed Use Kamloops case), buildingLocation now gets that value
- If a project has neither, buildingLocation remains undefined (no behavior change)

Do NOT modify any other field. Do NOT change the alignment of surrounding fields (buildingTag on line 168, handing on line 170). Preserve the exact whitespace style: 6-space leading indent, `??` separator with single spaces, trailing comma.

If TypeScript complains that `row.buildingArea` does not exist on the row type, read the row type definition (the parameter type of transformDoors — likely `DoorScheduleRow` from `services/doorScheduleService.ts`) and confirm `buildingArea` is declared. If declared as `string | undefined` or similar, no cast is needed — the `??` chain handles undefined naturally. If the type declaration is missing buildingArea entirely, do NOT add a cast; instead STOP and report this as a deeper type definition issue requiring discussion (it would indicate the type-DB sync is broken).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('utils/hardwareTransformers.ts','utf8');const lines=c.split('\n');const line=lines.find(l=>l.includes('buildingLocation:')&&l.includes('BUILDING LOCATION'));if(!line){console.error('buildingLocation line not found');process.exit(1);}if(!line.includes('row.buildingArea')){console.error('row.buildingArea fallback missing. Line:',line);process.exit(1);}if(!line.includes('row.buildingLocation')){console.error('row.buildingLocation existing fallback removed. Line:',line);process.exit(1);}const idxBA=line.indexOf('row.buildingArea');const idxBL=line.indexOf('row.buildingLocation');if(idxBA<idxBL){console.error('row.buildingArea must come AFTER row.buildingLocation in the fallback chain');process.exit(1);}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `utils/hardwareTransformers.ts` contains the literal substring `row.buildingArea` on the buildingLocation assignment line (verifiable: `grep -n "row.buildingArea" utils/hardwareTransformers.ts` returns at least one match)
    - The buildingLocation line still contains `bi?.['BUILDING LOCATION']`, `d?.['BUILDING LOCATION']`, and `row.buildingLocation` in that order before `row.buildingArea` (verifiable: the line matches regex `bi\?.*BUILDING LOCATION.*d\?.*BUILDING LOCATION.*row\.buildingLocation.*row\.buildingArea`)
    - The `??` operator is used (not `||`) for the new fallback (verifiable: line contains `?? row.buildingArea`, not `|| row.buildingArea`)
    - No other line in the file is modified (verifiable: `git diff utils/hardwareTransformers.ts` shows exactly one changed line containing the buildingLocation assignment)
    - TypeScript compiles without new errors: `npm run build` exits 0 OR exits with only pre-existing errors unrelated to this change
  </acceptance_criteria>
  <done>
    Line 169 fallback chain ends with `?? row.buildingArea`. Git diff shows exactly one line changed. Build does not produce new errors mentioning buildingLocation or buildingArea.
  </done>
</task>

</tasks>

<verification>
Phase-level checks:
1. `grep -n "row.buildingArea" utils/hardwareTransformers.ts` returns the buildingLocation line
2. The fallback ordering is preserved: bi -> d -> row.buildingLocation -> row.buildingArea
3. `npm run build` succeeds (no new TypeScript errors)
</verification>

<success_criteria>
- Server-loaded projects with `DoorScheduleRow.buildingArea` populated now produce Doors with `buildingLocation` set
- Existing projects with `buildingLocation` set behave unchanged (priority preserved)
- No type errors from accessing row.buildingArea
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-02-SUMMARY.md`
</output>

---
phase: 03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/pricing/PricingReportConfig.tsx
autonomous: true
requirements:
  - PRF-06
must_haves:
  truths:
    - "When a level filter is active, exported Excel/PDF Pricing Report contains ONLY the filtered door groups (matches visibleDoors)"
    - "When a level filter is active, exported Excel/PDF Pricing Report contains ONLY the filtered frame groups (matches visibleFrames)"
    - "When no filter is active, export contains all groups (visibleDoors === doorGroups in elements -> no regression)"
    - "Proposal tab tables continue to render unfiltered doorGroups/frameGroups (Proposal is intentionally unfiltered)"
  artifacts:
    - path: "components/pricing/PricingReportConfig.tsx"
      provides: "usePricingExport invocation receives visibleDoors and visibleFrames (filtered) instead of doorGroups and frameGroups (unfiltered)"
      contains: "doorGroups: visibleDoors"
  key_links:
    - from: "components/pricing/PricingReportConfig.tsx"
      to: "hooks/usePricingExport.ts"
      via: "filtered data passed as doorGroups/frameGroups arguments to export hook"
      pattern: "doorGroups: visibleDoors"
---

<objective>
Fix Bug C — `PricingReportConfig.tsx` calls `usePricingExport` with `doorGroups` and `frameGroups` (the raw unfiltered sets) on lines 172-173, while the UI table renders `visibleDoors` and `visibleFrames` (the filtered sets). Result: when the user applies a level filter and exports, the exported file contains rows that are not visible in the UI. Hardware (line 174) is already correct.

Purpose: Enables PRF-06 (export matches filtered UI). Critical: must NOT change the Proposal tab tables (lines ~371, 418-434, 461-479) which intentionally use unfiltered `doorGroups`/`frameGroups` for proposal totals.
Output: Two changed lines (172, 173) in `components/pricing/PricingReportConfig.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-CONTEXT.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-RESEARCH.md
@components/pricing/PricingReportConfig.tsx
@hooks/usePricingExport.ts

<interfaces>
<!-- Existing usePricingExport invocation (lines 168-198 of components/pricing/PricingReportConfig.tsx). -->
<!-- Bug at lines 172-173. Hardware on line 174 already correct (uses visibleHardware). -->
<!-- visibleDoors and visibleFrames are typed as DoorPricingGroup[], same as doorGroups/frameGroups. -->

Current (lines 168-198):
```typescript
const { handleDownloadExcel, handleDownloadPdf, handleDownloadProposalPdf } = usePricingExport({
  projectId,
  projectName,
  companySettings,
  doorGroups,                          // ← BUG (unfiltered)
  frameGroups,                         // ← BUG (unfiltered)
  hardwareGroups: visibleHardware,     // ← correct (filtered)
  doorTotal,
  frameTotal,
  ...
});
```

usePricingExport accepts the export data; it does not re-filter. The argument type `DoorPricingGroup[]` is identical for both filtered and unfiltered groups.

CRITICAL — DO NOT MODIFY:
- Lines around 371, 418-434, 461-479: Proposal tab table iteration uses `doorGroups`/`frameGroups` directly. These must remain unfiltered.
- proposalDoorBase, proposalFrameBase: derived from unfiltered groups via calcTotal. Unchanged.
- doorTotal, frameTotal: already derived from visibleDoors/visibleFrames. Unchanged.
- hwSetList: derived from unfiltered hardwareGroups + all doors. Unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace doorGroups and frameGroups with visibleDoors and visibleFrames in usePricingExport invocation</name>
  <files>components/pricing/PricingReportConfig.tsx</files>
  <read_first>
    - components/pricing/PricingReportConfig.tsx (read lines 1-50 to confirm import of usePricingExport, then lines 100-200 to confirm visibleDoors/visibleFrames are destructured from usePricingFilters and that doorGroups/frameGroups are also available — both must be in scope; then lines 168-198 to confirm exact current invocation; then search for OTHER uses of doorGroups and frameGroups to confirm they remain in scope for Proposal tab — grep for `doorGroups` and `frameGroups` within the file)
    - hooks/usePricingExport.ts (confirm the param shape — first lines of the hook should reveal the props interface; verify doorGroups parameter is typed `DoorPricingGroup[]` so passing visibleDoors of the same type is type-safe)
    - hooks/usePricingFilters.ts (lines 200-230 to confirm visibleDoors and visibleFrames are exported from the hook and have type DoorPricingGroup[])
    - .planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-RESEARCH.md (Bug C section lines 169-180 and Pitfalls section lines 318-332)
  </read_first>
  <action>
Open `components/pricing/PricingReportConfig.tsx`. Locate the `usePricingExport` invocation that begins around line 168.

The current code (lines 168-174) reads:

```typescript
  const { handleDownloadExcel, handleDownloadPdf, handleDownloadProposalPdf } = usePricingExport({
    projectId,
    projectName,
    companySettings,
    doorGroups,
    frameGroups,
    hardwareGroups: visibleHardware,
```

Change EXACTLY two lines (172 and 173):

```typescript
  const { handleDownloadExcel, handleDownloadPdf, handleDownloadProposalPdf } = usePricingExport({
    projectId,
    projectName,
    companySettings,
    doorGroups: visibleDoors,
    frameGroups: visibleFrames,
    hardwareGroups: visibleHardware,
```

The transformation:
- `doorGroups,` (shorthand) -> `doorGroups: visibleDoors,` (renamed property)
- `frameGroups,` (shorthand) -> `frameGroups: visibleFrames,` (renamed property)

The KEY (`doorGroups`, `frameGroups`) is the parameter name expected by `usePricingExport`. Do NOT change the keys.
The VALUE was the local variable `doorGroups`/`frameGroups` (shorthand); now becomes the local variable `visibleDoors`/`visibleFrames`.

Preserve indentation (4 spaces leading, based on the existing block style).
Preserve the trailing comma on each line.
Do NOT modify line 174 (`hardwareGroups: visibleHardware,` — already correct).
Do NOT modify any other line in the file.

DO NOT TOUCH the Proposal tab table renderers (search the file for other occurrences of `doorGroups` and `frameGroups` — they appear in the JSX around lines 371, 418-434, 461-479; those must remain `doorGroups`/`frameGroups` to render unfiltered Proposal data).

Variable scope confirmation: `visibleDoors` and `visibleFrames` MUST be already destructured from `usePricingFilters(...)` earlier in the component. If they are not, this fix cannot proceed without a separate change to add them — read the destructuring of `usePricingFilters` and confirm `visibleDoors` and `visibleFrames` are present. If absent, STOP and report.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('components/pricing/PricingReportConfig.tsx','utf8');if(!c.includes('doorGroups: visibleDoors')){console.error('Missing: doorGroups: visibleDoors');process.exit(1);}if(!c.includes('frameGroups: visibleFrames')){console.error('Missing: frameGroups: visibleFrames');process.exit(1);}if(!c.includes('hardwareGroups: visibleHardware')){console.error('Regression: hardwareGroups: visibleHardware missing');process.exit(1);}const exportBlockMatch=c.match(/usePricingExport\(\{[\s\S]*?\}\)/);if(!exportBlockMatch){console.error('usePricingExport call not found');process.exit(1);}const block=exportBlockMatch[0];if(/^\s*doorGroups,\s*$/m.test(block)){console.error('Old shorthand doorGroups, still present in usePricingExport call');process.exit(1);}if(/^\s*frameGroups,\s*$/m.test(block)){console.error('Old shorthand frameGroups, still present in usePricingExport call');process.exit(1);}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal string `doorGroups: visibleDoors` (verifiable: `grep -n "doorGroups: visibleDoors" components/pricing/PricingReportConfig.tsx` returns exactly one match in the usePricingExport block)
    - File contains the literal string `frameGroups: visibleFrames` (verifiable: `grep -n "frameGroups: visibleFrames" components/pricing/PricingReportConfig.tsx` returns exactly one match)
    - File still contains `hardwareGroups: visibleHardware` (verifiable: grep returns a match — regression check)
    - Within the usePricingExport invocation block, the shorthand `doorGroups,` (alone on a line) NO LONGER appears (verifiable: regex check inside the export block)
    - Within the usePricingExport invocation block, the shorthand `frameGroups,` (alone on a line) NO LONGER appears
    - The variable names `doorGroups` and `frameGroups` STILL appear elsewhere in the file (Proposal tab JSX) — count must be at least 2 occurrences each outside the usePricingExport call (verifiable: `grep -c doorGroups components/pricing/PricingReportConfig.tsx` returns >= 3)
    - Git diff shows exactly two changed lines in components/pricing/PricingReportConfig.tsx (verifiable: `git diff --stat components/pricing/PricingReportConfig.tsx` shows 2 insertions + 2 deletions)
    - Build succeeds: `npm run build` exits 0 OR exits with only pre-existing errors unrelated to this change
  </acceptance_criteria>
  <done>
    usePricingExport receives visibleDoors and visibleFrames as doorGroups/frameGroups respectively. Proposal tab JSX still references unfiltered doorGroups/frameGroups. Build succeeds.
  </done>
</task>

</tasks>

<verification>
Phase-level checks:
1. `grep "doorGroups: visibleDoors" components/pricing/PricingReportConfig.tsx` returns one match
2. `grep "frameGroups: visibleFrames" components/pricing/PricingReportConfig.tsx` returns one match
3. `grep -c "doorGroups" components/pricing/PricingReportConfig.tsx` returns >= 3 (Proposal tab still references it)
4. `npm run build` succeeds
</verification>

<success_criteria>
- Export hook receives filtered data
- Proposal tab unaffected (still uses unfiltered doorGroups/frameGroups)
- Hardware export already correct, unchanged
- No regression: doorTotal/frameTotal already derived from visibleDoors/visibleFrames
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-03-SUMMARY.md`
</output>

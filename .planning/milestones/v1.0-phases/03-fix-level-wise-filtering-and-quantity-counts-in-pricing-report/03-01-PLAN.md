---
phase: 03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/xlsxParser.ts
autonomous: true
requirements:
  - PRF-01
  - PRF-03
must_haves:
  truths:
    - "Client-side Excel uploads with column header 'BUILDINGN AREA' produce Door records with buildingLocation populated (not undefined)"
    - "Client-side Excel uploads with column header 'BUILDING AREA' produce Door records with buildingLocation populated"
    - "Existing column header 'BUILDING LOCATION' continues to map to buildingLocation (no regression)"
  artifacts:
    - path: "utils/xlsxParser.ts"
      provides: "Header alias mapping for buildingLocation includes 'buildingn area', 'building area', 'buildingarea'"
      contains: "buildingn area"
  key_links:
    - from: "utils/xlsxParser.ts"
      to: "utils/pricingGrouping.ts"
      via: "Door.buildingLocation populated -> group.floors[] populated -> filter UI shows level options"
      pattern: "buildingn area"
---

<objective>
Fix Bug A — `xlsxParser.ts` does not recognize the "BUILDINGN AREA" or "BUILDING AREA" Excel column as `buildingLocation`. The Mixed Use Kamloops Excel uses "BUILDINGN AREA" (typo from source) which normalizes to `buildingnarea` and matches no alias, causing every door's `buildingLocation` to be `undefined`. The level filter dropdown then shows no options.

Purpose: Enables PRF-01 (level filter) and PRF-03 (Kamloops correctness) for the client-side Excel upload code path.
Output: Updated alias list in `utils/xlsxParser.ts` line 73.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-CONTEXT.md
@.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-RESEARCH.md
@utils/xlsxParser.ts

<interfaces>
<!-- Existing alias structure in utils/xlsxParser.ts (lines 69-95). The fix extends one line. -->
<!-- The normalizer normalizeHeader() lowercases input and strips non-alphanumerics. -->
<!-- 'BUILDINGN AREA' normalizes to 'buildingnarea'; 'BUILDING AREA' normalizes to 'buildingarea'. -->

Current (line 73):
```typescript
buildingLocation:     ['buildinglocation', 'building location', 'bldg location', 'bldglocation'],
```

Aliases are matched against the normalized form of the Excel column header. Each alias string in the array is compared after both sides are normalized via `normalizeHeader()`. Adding the human-readable form ('buildingn area', 'building area') is sufficient because the comparator normalizes both operands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 'buildingn area', 'building area', 'buildingarea' aliases for buildingLocation</name>
  <files>utils/xlsxParser.ts</files>
  <read_first>
    - utils/xlsxParser.ts (read lines 65-100 to confirm exact current content of headerMappings.buildingLocation array, then read the normalizeHeader function definition to confirm normalization semantics)
    - .planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-RESEARCH.md (Bug A section, lines 147-156, for root cause and exact expected aliases)
  </read_first>
  <action>
Open `utils/xlsxParser.ts`. Locate line 73 which currently reads:

```typescript
buildingLocation:     ['buildinglocation', 'building location', 'bldg location', 'bldglocation'],
```

Replace with:

```typescript
buildingLocation:     ['buildinglocation', 'building location', 'bldg location', 'bldglocation', 'buildingn area', 'building area', 'buildingarea'],
```

Three new aliases are appended (no existing aliases removed):
1. `'buildingn area'` — exact form of typo column in Mixed Use Kamloops source
2. `'building area'` — corrected spelling for any future Excel sheets
3. `'buildingarea'` — already-normalized form (defensive; matches if header arrives pre-normalized)

Do NOT change any other line. Do NOT reorder existing aliases. Do NOT add hyphenated variants — the normalizer strips hyphens, so `'buildingn-area'` would normalize identically to `'buildingnarea'` already covered.

Preserve existing alignment/spacing within the file (the existing array uses single quotes, comma-space separators, and a leading 4-space indent — match this exactly).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('utils/xlsxParser.ts','utf8');const m=c.match(/buildingLocation:\s*\[([^\]]+)\]/);if(!m)process.exit(1);const arr=m[1];['buildingn area','building area','buildingarea','buildinglocation','building location','bldg location','bldglocation'].forEach(a=>{if(!arr.includes(\"'\"+a+\"'\")){console.error('Missing alias: '+a);process.exit(1);}});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `utils/xlsxParser.ts` contains the literal string `'buildingn area'` inside the buildingLocation alias array (verifiable: `grep -n "'buildingn area'" utils/xlsxParser.ts` returns line 73)
    - File contains `'building area'` (verifiable: `grep -n "'building area'" utils/xlsxParser.ts` returns a match on line 73)
    - File contains `'buildingarea'` (verifiable: `grep -n "'buildingarea'" utils/xlsxParser.ts` returns a match on line 73)
    - File still contains all four original aliases: `'buildinglocation'`, `'building location'`, `'bldg location'`, `'bldglocation'` (verifiable: each grep returns a match)
    - The buildingLocation array is on a single line and exists in the headerMappings object (verifiable: `grep -nE "buildingLocation:\s*\[" utils/xlsxParser.ts` returns exactly one match at line 73)
    - No other line in the file is modified (verifiable: `git diff utils/xlsxParser.ts` shows exactly one changed line, line 73)
  </acceptance_criteria>
  <done>
    Line 73 of utils/xlsxParser.ts contains the 7 aliases listed above. Git diff shows exactly one line changed. Project builds with `npm run build` without new errors related to xlsxParser.
  </done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:
1. `grep "'buildingn area'" utils/xlsxParser.ts` returns line 73
2. `grep "'building area'" utils/xlsxParser.ts` returns line 73
3. `npm run lint` passes for utils/xlsxParser.ts
4. Build succeeds: `npm run build`
</verification>

<success_criteria>
- buildingLocation alias array contains 7 entries (4 original + 3 new)
- Original aliases preserved verbatim
- New aliases use natural lowercase + space form (not normalized form, not hyphenated form)
- File compiles, lint passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-level-wise-filtering-and-quantity-counts-in-pricing-report/03-01-SUMMARY.md`
</output>

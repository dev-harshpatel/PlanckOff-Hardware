---
phase: 04-implement-real-time-ui-updates-via-supabase-realtime
plan: 08
type: execute
wave: 3
depends_on: ["04-05", "04-06", "04-07"]
files_modified:
  - .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md
autonomous: false
requirements: [RT-01, RT-02, RT-03, RT-04, RT-05, RT-06, RT-07, RT-08]
must_haves:
  truths:
    - "Migration 019 has been applied to the live Supabase project (publication contains all 5 tables)"
    - "Multi-tab change in Tab A appears in Tab B within 1-2 seconds for all 5 tables"
    - "Self-event filtering works (Tab A's own write does not double-update its own state)"
    - "Optimistic update rollback restores previous state and shows error toast"
    - "Reconnect after manual disconnect triggers a full reload"
    - "No stale subscription warnings or memory growth across navigation"
    - "Production build (npm run build) succeeds"
  artifacts:
    - path: ".planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md"
      provides: "Filled verification log with PASS/FAIL for each acceptance criterion + screenshots/evidence"
      contains: "RT-01"
  key_links:
    - from: "04-08-VERIFICATION.md"
      to: "All RT-01 through RT-08 requirements"
      via: "Acceptance criteria checklist"
      pattern: "RT-0[1-8]"
---

<objective>
Run the end-to-end verification for Phase 4. This is the gate that confirms the entire phase landed correctly. The plan has two automated tasks (build + grep audit of dedup wiring) and one human-verification checkpoint covering the multi-tab and reconnect scenarios.

Why a dedicated verification plan: Every prior plan (04-01 through 04-07) is structurally testable with `tsc --noEmit` and grep-based acceptance criteria, but the FUNCTIONAL behaviour of Realtime can only be observed by running the app and watching events arrive. This plan is the single point where that observation happens.

Output: A new `04-08-VERIFICATION.md` log filled in by the human verifier (or by Claude after running the manual steps with the user's help).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-CONTEXT.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-RESEARCH.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-01-PLAN.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-02-PLAN.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-03-PLAN.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-04-PLAN.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-05-PLAN.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-06-PLAN.md
@.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-07-PLAN.md

<interfaces>
<!-- The 8 phase requirements (from ROADMAP.md and RESEARCH.md): -->
<!-- RT-01: Any update made by the user is reflected in the UI instantly without page reload -->
<!-- RT-02: Multi-tab — change in Tab A reflected in Tab B within 1-2s -->
<!-- RT-03: Optimistic updates roll back cleanly on server failure with error message -->
<!-- RT-04: No stale subscriptions or memory leaks on navigation -->
<!-- RT-05: Cached data does not override fresh Realtime update -->
<!-- RT-06: Subscription reconnection on network drop is graceful -->
<!-- RT-07: Self-events from current tab do not cause double-updates -->
<!-- RT-08: All 5 tables in supabase_realtime publication (verified via SQL) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated audit — production build + grep-based wiring sanity checks</name>
  <files>.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-02-PLAN.md (channel + listener wiring expectations)
    - .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-05-PLAN.md (markPendingWrite hardware-finals expectation)
    - .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-06-PLAN.md (markPendingWrite pricing + projects expectations)
    - hooks/useProjectRealtime.ts (post-04-02 state)
    - hooks/useProjectData.ts (post-04-05 + 04-06 + 04-07 state)
    - hooks/useProjectPersistence.ts (post-04-05 state)
    - components/pricing/PricingReportConfig.tsx (post-04-06 state)
    - hooks/usePricingProposal.ts (post-04-06 state)
    - contexts/ProjectContext.tsx (post-04-06 state)
    - lib/realtime/dedupSet.ts (post-04-03 state)
    - hooks/useOptimisticDoorWrite.ts (post-04-07 state)
  </read_first>
  <action>
    Create `.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` with the structure below. Run each automated check via Bash and record PASS / FAIL plus the actual command output for each row. Treat any FAIL as blocking — do not advance to Task 2 until all automated checks pass.

    Initial template content (executor will fill in results):

    ```markdown
    # Phase 4 Verification Log

    **Phase:** 04-implement-real-time-ui-updates-via-supabase-realtime
    **Verified:** {{ISO date when this plan runs}}
    **Verifier:** {{Claude session id or human name}}

    ## Automated Checks

    | # | Check | Command | Expected | Actual | Result |
    |---|-------|---------|----------|--------|--------|
    | A1 | TypeScript compile | `npx tsc --noEmit` | Exit 0 | _fill in_ | _PASS/FAIL_ |
    | A2 | Production build | `npm run build` | Exit 0 | _fill in_ | _PASS/FAIL_ |
    | A3 | Migration 019 exists | `node -e "require('fs').statSync('supabase/migrations/019_enable_realtime_pricing_projects.sql')"` | No throw | _fill in_ | _PASS/FAIL_ |
    | A4 | dedupSet.ts exists with 3 exports | `node -e "const c=require('fs').readFileSync('lib/realtime/dedupSet.ts','utf8'); ['markPendingWrite','isOwnWrite','__clearPendingWritesForTest'].forEach(n=>{ if(!new RegExp('export function '+n).test(c)) throw new Error(n)})"` | No throw | _fill in_ | _PASS/FAIL_ |
    | A5 | useProjectRealtime has 5 .on() listeners | grep `.on(` count in hooks/useProjectRealtime.ts | 5 | _fill in_ | _PASS/FAIL_ |
    | A6 | useProjectRealtime subscribes to all 5 tables | grep `table: 'door_schedule_imports'`, `table: 'project_hardware_finals'`, `table: 'project_pricing_items'`, `table: 'project_pricing_proposal'`, `table: 'projects'` in hooks/useProjectRealtime.ts | each found | _fill in_ | _PASS/FAIL_ |
    | A7 | useProjectRealtime uses `id=eq.` (not `project_id=eq.`) for projects table | grep `filter: \`id=eq.${projectId}\`` in hooks/useProjectRealtime.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A8 | markPendingWrite called for project_hardware_finals | grep `markPendingWrite\('project_hardware_finals'` in hooks/useProjectPersistence.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A9 | markPendingWrite called for project_pricing_items | grep `markPendingWrite\('project_pricing_items'` in components/pricing/PricingReportConfig.tsx | 1 match | _fill in_ | _PASS/FAIL_ |
    | A10 | markPendingWrite called for project_pricing_proposal | grep `markPendingWrite\('project_pricing_proposal'` in hooks/usePricingProposal.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A11 | markPendingWrite called for projects | grep `markPendingWrite\('projects'` in contexts/ProjectContext.tsx | 1 match | _fill in_ | _PASS/FAIL_ |
    | A12 | isOwnWrite called for project_hardware_finals | grep `isOwnWrite\('project_hardware_finals'` in hooks/useProjectData.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A13 | isOwnWrite called for project_pricing_items | grep `isOwnWrite\('project_pricing_items'` in components/pricing/PricingReportConfig.tsx | 1 match | _fill in_ | _PASS/FAIL_ |
    | A14 | isOwnWrite called for project_pricing_proposal | grep `isOwnWrite\('project_pricing_proposal'` in hooks/usePricingProposal.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A15 | isOwnWrite called for projects | grep `isOwnWrite\('projects'` in contexts/ProjectContext.tsx | 1 match | _fill in_ | _PASS/FAIL_ |
    | A16 | useOptimisticDoorWrite hook exists | grep `^export function useOptimisticDoorWrite` in hooks/useOptimisticDoorWrite.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A17 | Auto-save loop suppression order in useProjectData hardware-finals callback | Read hooks/useProjectData.ts handleHardwareFinalsChange — `isInitialMount.current = true` line precedes `setHardwareSets(` line | true | _fill in_ | _PASS/FAIL_ |
    | A18 | Auto-save loop suppression in optimistic rollback | Read hooks/useOptimisticDoorWrite.ts catch block — `isInitialMount.current = true` line precedes `setter(prev)` line | true | _fill in_ | _PASS/FAIL_ |
    | A19 | reloadAllProjectData wired as onFullReload | grep `onFullReload:\s*reloadAllProjectData` in hooks/useProjectData.ts | 1 match | _fill in_ | _PASS/FAIL_ |
    | A20 | Single channel name pattern preserved | grep `\`project-realtime-\${projectId}\`` in hooks/useProjectRealtime.ts | 1 match | _fill in_ | _PASS/FAIL_ |

    ## Manual Multi-Tab Tests (Task 2)

    _Filled in during Task 2 — DO NOT proceed past Task 1 if any A* check above fails._

    {{checkpoint section appended by Task 2}}

    ## Result

    {{Final PASS/FAIL summary — appended by Task 2 close-out}}
    ```

    Run each automated check, capture the actual output, and fill in the table. If A2 (`npm run build`) fails, do NOT continue — surface the failure to the user (it likely indicates a TypeScript regression introduced by an incomplete prior plan).

    For grep-based checks, use:
    ```bash
    grep -c "PATTERN" path/to/file
    ```
    and record the count. For checks where the expected count is "≥1", any non-zero is PASS.

    For A17 and A18 (ordering checks), read the file and confirm by line numbers that the auto-save guard precedes the setState.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md','utf8');const fails=(c.match(/FAIL/g)||[]).length;const passes=(c.match(/PASS/g)||[]).length;console.log(`fails=${fails} passes=${passes}`);if(fails>0){console.error('Verification has FAIL rows — block on Task 2');process.exit(1)}if(passes<20){console.error('Fewer than 20 PASS rows recorded — incomplete');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` exists
    - File contains all 20 automated check rows (A1 through A20) — `grep -c "^| A" .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` returns 20
    - Every check row has been filled with an Actual column value (no `_fill in_` placeholders remain in the Actual column): `grep -c "_fill in_" .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` returns 0
    - Every check row records PASS or FAIL — no row has only `_PASS/FAIL_`
    - The number of PASS rows is at least 20 (every automated check passed): `grep -c "PASS$" .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` returns at least 20
    - The number of FAIL rows is exactly 0: `grep -c "FAIL$" .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` returns 0
    - `npm run build` actually succeeded (the file's A2 row records actual exit-0 output)
  </acceptance_criteria>
  <done>The verification file exists with all automated checks recorded as PASS. Any FAIL would have blocked progression to Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify multi-tab Realtime sync, optimistic rollback, and reconnect recovery</name>
  <files>.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md</files>
  <action>Pause execution and wait for the human verifier. Provide the steps below (see <how-to-verify>) and append the test outcomes (PASS/FAIL/N/A) to .planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md under a new section ## Manual Multi-Tab Tests. Resume only after the verifier types approved or names a failing test ID.</action>
  <verify><automated>node -e "const c=require('fs').readFileSync('.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md','utf8');if(!/## Manual Multi-Tab Tests/.test(c))throw new Error('missing manual section');const fails=(c.match(/| FAIL/g)||[]).length;if(fails>0)throw new Error('FAIL rows present');console.log('OK')"</automated></verify>
  <done>All manual tests M1-M10 (M7 optionally N/A) recorded as PASS in 04-08-VERIFICATION.md and the verifier issued the approved resume signal.</done>
  <what-built>
    All Phase 4 plumbing is in place and structurally verified by Task 1: 5-table Realtime subscription, dedup module, in-memory patch callbacks, optimistic-write hook. Task 2 is the functional verification — these behaviours can only be confirmed by running the app and observing events arrive.
  </what-built>
  <how-to-verify>
    Append the results of every test below to `.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-VERIFICATION.md` under a new section `## Manual Multi-Tab Tests`. Each test gets a row in a table with columns: # | Test | Steps | Expected | Actual | Result.

    **Pre-conditions (must be true before running tests):**
    1. Migration 019 has been applied to the live Supabase project. Verify by running this SQL in the Supabase SQL editor:
       ```sql
       SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime' ORDER BY tablename;
       ```
       Expected output includes: `door_schedule_imports`, `project_hardware_finals`, `project_pricing_items`, `project_pricing_proposal`, `projects`. If any of the last three are missing, the user must run `supabase db push` (or apply the migration manually via the dashboard) before continuing.
    2. The dev server is running: `npm run dev` (or the user's preferred command).
    3. A test project exists in the database with at least one hardware set, one door, and pricing data. (The Mixed Use Kamloops project is acceptable.)

    **Test M1 — RT-08 publication audit (sanity):**
    - Steps: Run the SQL from pre-condition #1.
    - Expected: All 5 table names present in the result.
    - If FAIL: User must apply migration 019 and re-run.

    **Test M2 — RT-01 + RT-02: Door edit propagates Tab A → Tab B (project_hardware_finals):**
    - Steps:
      1. Open the test project in two browser tabs (Tab A, Tab B), same browser, same login.
      2. In Tab A, edit a door's tag (e.g. change `D-01` to `D-01X`).
      3. Observe Tab B WITHOUT refreshing.
    - Expected: Tab B's door list updates to show `D-01X` within 1-2 seconds. No page reload required.
    - Record the observed latency (use a stopwatch or mental count).

    **Test M3 — RT-01 + RT-02: Pricing edit propagates Tab A → Tab B (project_pricing_items):**
    - Steps:
      1. In Tab A, navigate to the Pricing report. Set the unit price for one door group to `100`.
      2. Wait ~1 second for the auto-save (600ms debounce + network).
      3. In Tab B (also on the Pricing report), observe the price input WITHOUT refreshing.
    - Expected: Tab B's price input shows `100` within 1-2 seconds.

    **Test M4 — RT-01 + RT-02: Profit% propagates Tab A → Tab B (project_pricing_proposal):**
    - Steps:
      1. In Tab A, set the door profit% to `25`.
      2. Wait ~1 second for the 800ms debounce + network.
      3. In Tab B (Proposal section), observe.
    - Expected: Tab B shows door profit% = `25` within 1-2 seconds.

    **Test M5 — RT-01 + RT-02: Project metadata propagates Tab A → Tab B (projects):**
    - Steps:
      1. In Tab A, edit the project's `dueDate` (or another metadata field).
      2. Save.
      3. In Tab B (project view header), observe the metadata.
    - Expected: Tab B shows the new value within 1-2 seconds.

    **Test M6 — RT-07: Self-event filtering (no double-update):**
    - Steps:
      1. Open the test project in a SINGLE tab.
      2. Open the browser devtools Network panel and filter to `realtime` (Supabase WebSocket frames).
      3. Edit a door tag.
      4. Observe both the UI and the Network panel.
    - Expected: The UI updates exactly ONCE (no flicker, no value momentarily reverting). The Network panel shows the Realtime echo arriving — but the UI does NOT visibly re-render a second time.
    - Implementation note: This test confirms that `isOwnWrite()` correctly skipped the echo. If the UI flickers, dedup is broken.

    **Test M7 — RT-03: Optimistic rollback on server error:**
    - Steps:
      1. In the dev console, temporarily monkey-patch `fetch` to fail PUT calls to `/api/projects/`:
         ```js
         const _fetch = window.fetch;
         window.fetch = async (url, opts) => {
           if (typeof url === 'string' && url.includes('/api/projects/') && opts?.method === 'PUT') {
             return new Response('Simulated 500', { status: 500 });
           }
           return _fetch(url, opts);
         };
         ```
      2. (Skip this test if no call site has yet adopted `useOptimisticDoorWrite`. Plan 04-07 explicitly says adoption is opt-in for future PRs.) If a call site HAS adopted it, edit a door and observe.
    - Expected (if a call site uses optimistic write): UI shows the new value briefly, then reverts to the prior value, then an error toast appears reading "Save failed.".
    - Expected (if no call site has adopted yet): mark as N/A — the hook is exposed but unused. Note in the verification log that adoption is deferred to a future PR.

    **Test M8 — RT-04: No stale subscriptions on navigation:**
    - Steps:
      1. Open the test project.
      2. Open browser devtools → Memory → take a heap snapshot (snapshot 1).
      3. Navigate to a different project.
      4. Navigate back to the test project.
      5. Repeat steps 3-4 four more times (5 total round trips).
      6. Take another heap snapshot (snapshot 2).
    - Expected: The number of `RealtimeChannel` instances retained between snapshots does not grow. (Search the snapshots for "RealtimeChannel" or use the Comparison view.) Memory growth should be bounded by detached DOM and React Fibers, not by Realtime channels.
    - Pragmatic check: If browser devtools heap inspection is too involved, use the Network panel — count active WebSocket connections (`wss://`). Should be 1 (Supabase) regardless of how many times you navigated.

    **Test M9 — RT-06: Reconnect recovery triggers full reload:**
    - Steps:
      1. Open the test project. Open the Network panel; filter to `wss`.
      2. Right-click the Supabase WebSocket connection and choose "Block request URL" (or use the offline toggle).
      3. Wait 5-10 seconds.
      4. Unblock / go back online.
      5. In another browser tab WHILE OFFLINE, edit a door (this generates an event Tab A will miss).
      6. After Tab A reconnects, observe.
    - Expected: Tab A's WebSocket re-establishes, channel status goes CLOSED → SUBSCRIBED, the `onFullReload` callback fires, `reloadAllProjectData()` runs, and the door edit made during the offline window appears in Tab A. There is no manual reload required.

    **Test M10 — RT-05: Stale-cache regression:**
    - Steps:
      1. In Tab A, perform any edit that triggers a Realtime event (e.g. a price change).
      2. In Tab B, observe.
      3. After Tab B updates, immediately reload Tab B.
      4. Confirm Tab B still shows the edited value (proves the DB has it).
    - Expected: No "split-brain" — Tab B never shows a stale value after the Realtime event arrived. (This codebase has no SWR/React Query, so this test essentially confirms the in-memory patch is also persisted via the auto-save loop on the source tab.)

    **Pass/fail rule:** Tests M1, M2, M3, M4, M5, M6, M8, M9, M10 must PASS (M7 may be N/A). Any FAIL blocks the phase.

    Append a `## Result` section at the bottom of `04-08-VERIFICATION.md` summarising the full pass/fail outcome.
  </how-to-verify>
  <resume-signal>
    Type `approved` when all manual tests have been executed and recorded as PASS (or N/A for M7 if no adoption yet). Type the failing test ID(s) (e.g. `M3, M6`) to flag a regression that needs gap-closure planning.
  </resume-signal>
</task>

</tasks>

<verification>
1. Task 1 automated audit produces a verification log with ≥20 PASS rows and 0 FAIL rows.
2. Task 2 manual checkpoint records all 10 manual tests with PASS / FAIL / N/A.
3. The verification log ends with a `## Result` summary.
</verification>

<success_criteria>
- `04-08-VERIFICATION.md` exists with both automated and manual sections filled in
- All 8 RT-* requirements are demonstrably satisfied (or explicitly deferred with rationale, e.g. M7 N/A)
- Production build (`npm run build`) succeeds
- No FAIL row remains in the verification log
</success_criteria>

<output>
After completion, create `.planning/phases/04-implement-real-time-ui-updates-via-supabase-realtime/04-08-SUMMARY.md` documenting:
- The path to `04-08-VERIFICATION.md`
- A short narrative of which RT-* requirements were verified by which test
- Any deferred items (e.g. M7 if optimistic-write adoption is deferred — name the issue tracker entry where adoption is queued)
- Phase 4 final status: COMPLETE if all required tests pass, BLOCKED otherwise
- Update `.planning/STATE.md` to record phase 4 as Complete (or annotate the blocker)
</output>

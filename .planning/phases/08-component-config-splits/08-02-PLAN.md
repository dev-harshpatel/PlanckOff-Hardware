---
phase: 08-component-config-splits
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/hardware/HardwareSetConfig/index.tsx
  - components/hardware/HardwareSetConfig/hardwareConstants.ts
  - components/hardware/HardwareSetConfig/hardwareHelpers.ts
  - components/hardware/HardwareSetConfig/HardwareGroupTable.tsx
  - components/hardware/HardwareSetConfig.tsx
autonomous: true
requirements:
  - COMP-03
  - COMP-04
  - VER-02
  - VER-03

must_haves:
  truths:
    - "components/hardware/HardwareSetConfig/ sub-directory exists with index.tsx as the main component file"
    - "components/hardware/HardwareSetConfig/hardwareConstants.ts exists and exports REQUIRED_COLUMN_DEFS, GROUPING_OPTIONS, USAGE_OPTIONS (was lines 52-77 of flat file)"
    - "components/hardware/HardwareSetConfig/hardwareHelpers.ts exists and exports getDoorHwSetName, formatDoorTags, getItemValue, getUsageCellValue, buildSetGroups, buildDoorFieldGroups, buildHardwareGroups (was lines 80-269 of flat file)"
    - "components/hardware/HardwareSetConfig/HardwareGroupTable.tsx exists and exports HardwareGroupTable (was lines 273-396 of flat file)"
    - "components/hardware/HardwareSetConfig/index.tsx is the main component file containing useState/useMemo/useCallback orchestration, the inline download handler (D-07 — no hook extraction), and JSX return"
    - "components/hardware/HardwareSetConfig.tsx (flat file at 853 lines) is DELETED"
    - "Every sub-file that uses React hooks or browser APIs has 'use client' as the LITERAL FIRST LINE — VER-02"
    - "hardwareConstants.ts does NOT have 'use client' (pure data file — no hooks, no browser APIs)"
    - "hardwareHelpers.ts does NOT have 'use client' (pure functions only)"
    - "index.tsx barrel contains explicit `export default HardwareSetConfig` (consumer default imports must resolve) — VER-03"
    - "index.tsx contains `export type { HardwareSetExportConfig } from '../../../types/hardwareSetTypes'` — type re-export preserved per D-08"
    - "All 4 consumer files compile without modification — barrel resolves the same default import paths"
  artifacts:
    - path: "components/hardware/HardwareSetConfig/index.tsx"
      provides: "Main component file (barrel) — orchestration + inline handleDownload + JSX return"
      contains: "'use client'; export default HardwareSetConfig; export type { HardwareSetExportConfig }"
      max_lines: 500
    - path: "components/hardware/HardwareSetConfig/hardwareConstants.ts"
      provides: "Static config arrays: REQUIRED_COLUMN_DEFS, GROUPING_OPTIONS, USAGE_OPTIONS"
      exports: ["REQUIRED_COLUMN_DEFS", "GROUPING_OPTIONS", "USAGE_OPTIONS"]
      max_lines: 50
    - path: "components/hardware/HardwareSetConfig/hardwareHelpers.ts"
      provides: "Pure helper functions for hardware-set data shaping"
      exports: ["getDoorHwSetName", "formatDoorTags", "getItemValue", "getUsageCellValue", "buildSetGroups", "buildDoorFieldGroups", "buildHardwareGroups"]
      max_lines: 220
    - path: "components/hardware/HardwareSetConfig/HardwareGroupTable.tsx"
      provides: "HardwareGroupTable sub-component — renders one grouped table"
      exports: ["HardwareGroupTable"]
      max_lines: 160
    - path: "components/hardware/HardwareSetConfig.tsx"
      provides: "DELETED — replaced by sub-directory"
      contains: "FILE MUST NOT EXIST"
  key_links:
    - from: "components/hardware/HardwareSetConfig/index.tsx"
      to: "components/hardware/HardwareSetConfig/hardwareConstants"
      via: "import { REQUIRED_COLUMN_DEFS, GROUPING_OPTIONS, USAGE_OPTIONS } from './hardwareConstants'"
      pattern: "from ['\"]\\./hardwareConstants['\"]"
    - from: "components/hardware/HardwareSetConfig/index.tsx"
      to: "components/hardware/HardwareSetConfig/hardwareHelpers"
      via: "import { buildSetGroups, buildDoorFieldGroups, buildHardwareGroups, ... } from './hardwareHelpers'"
      pattern: "from ['\"]\\./hardwareHelpers['\"]"
    - from: "components/hardware/HardwareSetConfig/index.tsx"
      to: "components/hardware/HardwareSetConfig/HardwareGroupTable"
      via: "import { HardwareGroupTable } from './HardwareGroupTable'"
      pattern: "from ['\"]\\./HardwareGroupTable['\"]"
    - from: "components/hardware/HardwareSetConfig/HardwareGroupTable.tsx"
      to: "components/hardware/HardwareSetConfig/hardwareHelpers"
      via: "import { formatDoorTags, getUsageCellValue, getItemValue } from './hardwareHelpers'"
      pattern: "from ['\"]\\./hardwareHelpers['\"]"
    - from: "components/hardware/HardwareSetConfig/HardwareGroupTable.tsx"
      to: "components/hardware/HardwareSetConfig/hardwareConstants"
      via: "import { REQUIRED_COLUMN_DEFS } from './hardwareConstants'"
      pattern: "from ['\"]\\./hardwareConstants['\"]"
    - from: "components/hardware/HardwareSetConfig/index.tsx"
      to: "types/hardwareSetTypes (canonical type re-export per D-08)"
      via: "export type { HardwareSetExportConfig } from '../../../types/hardwareSetTypes'"
      pattern: "export type \\{ HardwareSetExportConfig \\} from ['\"]\\.\\./\\.\\./\\.\\./types/hardwareSetTypes"
---

<objective>
Split `components/hardware/HardwareSetConfig.tsx` (853 lines) into a sub-directory module `components/hardware/HardwareSetConfig/` containing four files: `index.tsx` (main component with inline handleDownload + JSX), `hardwareConstants.ts` (static config arrays), `hardwareHelpers.ts` (7 pure helper functions), and `HardwareGroupTable.tsx` (sub-component).

Purpose: The flat 853-line file mixes static config arrays, 7 pure helper functions, a sub-component (HardwareGroupTable), and a 454-line main component (which includes a non-trivial inline download handler). Splitting into a sub-directory per locked decisions D-05..D-08 keeps each file focused on a single domain while honoring D-07's preference to keep the download handler inline (no hook extraction for this component). Pure structural refactor — zero behavior change.

Per D-07: `index.tsx` may slightly exceed 300 lines. The main component block is naturally ~454 lines. Target: under 500 lines. Simpler structure over strict line limit, as user explicitly chose.

Output:
- `components/hardware/HardwareSetConfig/index.tsx` — main component + inline handleDownload + JSX
- `components/hardware/HardwareSetConfig/hardwareConstants.ts` — REQUIRED_COLUMN_DEFS, GROUPING_OPTIONS, USAGE_OPTIONS (lines 52-77 of flat file)
- `components/hardware/HardwareSetConfig/hardwareHelpers.ts` — 7 pure functions (lines 80-269 of flat file)
- `components/hardware/HardwareSetConfig/HardwareGroupTable.tsx` — sub-component (lines 273-396 of flat file)
- `components/hardware/HardwareSetConfig.tsx` — DELETED after the sub-directory compiles cleanly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-component-config-splits/08-CONTEXT.md
@.planning/phases/08-component-config-splits/08-RESEARCH.md
@.planning/tsc-baseline.txt
@components/hardware/HardwareSetConfig.tsx
@types/hardwareSetTypes.ts

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From types/hardwareSetTypes.ts (CANONICAL types — Phase 7 PRE-03 deliverable):
```typescript
export interface HardwareSetExportConfig { /* canonical export config shape */ }
```

From types (project root):
```typescript
export interface Door { /* ... */ }
export interface HardwareSet { /* ... */ }
export interface HardwareItem { /* ... */ }
```

Constants to extract (lines 52-77 of components/hardware/HardwareSetConfig.tsx — confirmed by read):
```typescript
const REQUIRED_COLUMN_DEFS = [
  { id: 'hw_set_name',  label: 'HW Set',        desc: '...' },
  { id: 'name',         label: 'Item Name',     desc: '...' },
  // ... 9 entries total
];
const GROUPING_OPTIONS: { id: GroupByOption; label: string; desc: string }[] = [ /* 7 entries */ ];
const USAGE_OPTIONS = [ /* 2 entries */ ];
```

Helper functions to extract (lines 80-269 of flat file — 7 functions):
- `getDoorHwSetName(door: Door): string | null` (starts ~line 88)
- `formatDoorTags(doorTags: string[]): string` (returns formatted string)
- `getItemValue(item: HardwareItem, key: string): string`
- `getUsageCellValue(...): string`
- `buildSetGroups(...): HardwareGroup[]`
- `buildDoorFieldGroups(...): HardwareGroup[]`
- `buildHardwareGroups(...): HardwareGroup[]`

These functions reference local types (`GroupByOption`, `HardwareItemUsage`, `HardwareGroup`) defined inside the flat file at lines 27-48. Those local types must MOVE to a location accessible by both `hardwareHelpers.ts` and the index.tsx. Recommended approach: declare them inside `hardwareHelpers.ts` and export them as named types so index.tsx + HardwareGroupTable.tsx can import them too. Specifically:

```typescript
// In hardwareHelpers.ts — exported types used by helpers and consumers
export type GroupByOption = 'set' | 'type' | 'manufacturer' | 'flat' | 'buildingTag' | 'buildingLocation' | 'doorMaterial';

export interface HardwareItemUsage {
  item: HardwareItem;
  doorTags: string[];
  totalQuantity: number;
  sets: string[];
  doorQuantitySum: number;
  doorMaterials: string[];
}

export interface HardwareGroup {
  label: string;
  items: HardwareItemUsage[];
  groupDoorTags?: string[];
  groupTotalQuantity?: number;
}
```

HardwareGroupTable props (lines 273-280 of flat file — read source to confirm exact signature). Typically:
```typescript
{
  group: HardwareGroup;
  groupKey: string;
  selectedColumns: string[];
  usageDisplay: string[];
  isCollapsed: boolean;
  onToggleCollapsed: (key: string) => void;
  // ... any other props the body uses
}
```

Consumer import patterns (MUST remain valid after split):
- `app/project/[id]/reports/hardware-set/page.tsx:17`: `const HardwareSetConfig = dynamic(() => import('@/components/hardware/HardwareSetConfig'), { ssr: false });` — resolves to index.tsx in the sub-directory.
- `components/reports/ReportGenerationCenter.tsx:5`: `import HardwareSetConfig from '../hardware/HardwareSetConfig';`
- `views/ReportsView.tsx:7`: `import HardwareSetConfig from '../components/hardware/HardwareSetConfig';`

Path-depth adjustments (sub-directory is ONE LEVEL DEEPER than the flat file):
- `from '../../types'` (flat) → `from '../../../types'` (sub-dir)
- `from '../../types/hardwareSetTypes'` (flat) → `from '../../../types/hardwareSetTypes'` (sub-dir)
- `from '../../services/...'` (flat) → `from '../../../services/...'` (sub-dir)
- `from '@/...'` alias paths — UNCHANGED at any depth
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create hardwareConstants.ts and hardwareHelpers.ts (pure files — no 'use client')</name>
  <files>
    components/hardware/HardwareSetConfig/hardwareConstants.ts
    components/hardware/HardwareSetConfig/hardwareHelpers.ts
  </files>
  <read_first>
    components/hardware/HardwareSetConfig.tsx (full file — confirm REQUIRED_COLUMN_DEFS / GROUPING_OPTIONS / USAGE_OPTIONS live at lines 52-77; pure helpers live at lines 80-269; local types GroupByOption / HardwareItemUsage / HardwareGroup live at lines 27-48)
    .claude/skills/code-standards/SKILL.md (constants rules — `as const`, named exports; service/util rules — JSDoc, pure functions)
  </read_first>
  <action>
    **File 1: components/hardware/HardwareSetConfig/hardwareConstants.ts**

    Do NOT add `'use client';` — this is a pure data file with no hooks and no browser APIs. Adding `'use client'` would be wrong per RESEARCH §Pattern 2.

    Contents:
    ```typescript
    import type { GroupByOption } from './hardwareHelpers';

    /** Required column definitions for the hardware-set report — id, display label, description. */
    export const REQUIRED_COLUMN_DEFS = [
        { id: 'hw_set_name',  label: 'HW Set',        desc: 'Hardware set name(s) this item belongs to' },
        { id: 'name',         label: 'Item Name',      desc: 'Hardware item name/description'            },
        { id: 'description',  label: 'Description',    desc: 'Detailed specifications'                   },
        { id: 'manufacturer', label: 'Manufacturer',   desc: 'Brand/supplier name'                       },
        { id: 'finish',       label: 'Finish',         desc: 'Color/coating specification'               },
        { id: 'qty_per_set',  label: 'Qty/Set',        desc: 'Raw item quantity per hardware set'        },
        { id: 'quantity',     label: 'Total',          desc: 'Multiplied quantity (qty × doors)'         },
        { id: 'usage',        label: 'Usage',          desc: 'Door tags or quantity for this item'       },
        { id: 'door_material',label: 'Door Material',  desc: 'Materials of doors using this item'        },
    ];

    /** Grouping mode options for the hardware-set report. */
    export const GROUPING_OPTIONS: { id: GroupByOption; label: string; desc: string }[] = [
        { id: 'set',              label: 'By Hardware Set',      desc: 'One table per hardware set'            },
        { id: 'type',             label: 'By Item Type',         desc: 'Group by category (Hinges, Locksets…)' },
        { id: 'manufacturer',     label: 'By Manufacturer',      desc: 'Group by brand/supplier'               },
        { id: 'buildingTag',      label: 'By Building Tag',      desc: 'Group by building identifier'          },
        { id: 'buildingLocation', label: 'By Building Location', desc: 'Group by building location/floor'      },
        { id: 'doorMaterial',     label: 'By Door Material',     desc: 'Group by door material type'           },
        { id: 'flat',             label: 'Flat List',            desc: 'No grouping, single table'             },
    ];

    /** Usage-display mode options — full tag list vs. count only. */
    export const USAGE_OPTIONS = [
        { id: 'all',   label: 'Show all door tags', example: '101, 102, 103, 201...' },
        { id: 'count', label: 'Show count only',    example: 'Used in 6 doors'       },
    ];
    ```

    Match the EXACT content of lines 52-77 from the flat file — values, ordering, comments. Do not re-order or modify entries.

    **File 2: components/hardware/HardwareSetConfig/hardwareHelpers.ts**

    Do NOT add `'use client';` — pure functions only, no hooks, no browser APIs.

    Header:
    ```typescript
    import type { Door, HardwareSet, HardwareItem } from '../../../types';
    ```

    Then export the local types that hardwareHelpers' functions return/accept (so HardwareGroupTable.tsx and index.tsx can re-import them):
    ```typescript
    /** Grouping mode key for the hardware-set report. */
    export type GroupByOption = 'set' | 'type' | 'manufacturer' | 'flat' | 'buildingTag' | 'buildingLocation' | 'doorMaterial';

    /** A single hardware item enriched with usage statistics across all doors. */
    export interface HardwareItemUsage {
        item: HardwareItem;
        doorTags: string[];
        totalQuantity: number;
        sets: string[];
        /** Sum of basic_information.quantity across all doors that use this item */
        doorQuantitySum: number;
        /** Unique door materials from all doors that use this item */
        doorMaterials: string[];
    }

    /** A group of related hardware items used together — by set, type, manufacturer, etc. */
    export interface HardwareGroup {
        label: string;
        items: HardwareItemUsage[];
        /** Door tags for the whole group — set only in "By Hardware Set" mode */
        groupDoorTags?: string[];
        /** Sum of basic_information.QUANTITY across all doors in the group */
        groupTotalQuantity?: number;
    }
    ```

    Then copy the 7 helper functions EXACTLY as they appear in lines 80-269 of the flat file, changing each from a local `function` to an exported `export function`:
    - `getDoorHwSetName` (line ~88)
    - `formatDoorTags`
    - `getItemValue`
    - `getUsageCellValue`
    - `buildSetGroups`
    - `buildDoorFieldGroups`
    - `buildHardwareGroups`

    For each function:
    - Keep the EXACT body — no logic changes, no renaming variables.
    - Keep the JSDoc comments above the function declaration.
    - Add `export` prefix to each `function` keyword.
    - If a helper references another helper, both are now in the same file (no import needed).
    - If a helper references `Door`, `HardwareSet`, `HardwareItem` from the project types, the top-level import already covers it.
    - If a helper references `REQUIRED_COLUMN_DEFS`, `GROUPING_OPTIONS`, or `USAGE_OPTIONS` (it shouldn't, but check), add `import { ... } from './hardwareConstants';` at the top. Avoid circular: hardwareConstants imports `type GroupByOption` from hardwareHelpers (types are erased at runtime — no circular runtime issue).

    **Formatting:** Use **2-space indent** per .claude/skills/code-standards/SKILL.md (Prettier-enforced project standard). Re-indent any 4-space body copied from the source. Accept the cosmetic diff against the source file.

    **Step C — Verify circularity is type-only:**
    `hardwareConstants.ts` imports `type GroupByOption` from `./hardwareHelpers` (uses `import type` or named-type import — both are erased at runtime). `hardwareHelpers.ts` does NOT import from `./hardwareConstants` (the type definitions are in hardwareHelpers itself). Therefore there is no runtime circular dependency. TypeScript handles type-only imports across file boundaries cleanly.

    If hardwareHelpers DOES need to reference a constant from hardwareConstants at runtime, declare a small inline equivalent inside hardwareHelpers and accept the duplication — but check the source first: the 7 helpers at lines 80-269 of the flat file should NOT reference REQUIRED_COLUMN_DEFS / GROUPING_OPTIONS / USAGE_OPTIONS (those constants are consumed in the main component, not in the pure helpers). Confirm by reading and only add imports if actually referenced.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('components/hardware/HardwareSetConfig/hardwareConstants.ts','utf8');const h=fs.readFileSync('components/hardware/HardwareSetConfig/hardwareHelpers.ts','utf8');if(/^'use client'/.test(c))throw new Error('hardwareConstants.ts MUST NOT have use client');if(/^'use client'/.test(h))throw new Error('hardwareHelpers.ts MUST NOT have use client');if(!/export const REQUIRED_COLUMN_DEFS/.test(c))throw new Error('REQUIRED_COLUMN_DEFS not exported');if(!/export const GROUPING_OPTIONS/.test(c))throw new Error('GROUPING_OPTIONS not exported');if(!/export const USAGE_OPTIONS/.test(c))throw new Error('USAGE_OPTIONS not exported');if(!/export type GroupByOption/.test(h))throw new Error('GroupByOption not exported from helpers');if(!/export interface HardwareItemUsage/.test(h))throw new Error('HardwareItemUsage not exported');if(!/export interface HardwareGroup/.test(h))throw new Error('HardwareGroup not exported');for(const fn of ['getDoorHwSetName','formatDoorTags','getItemValue','getUsageCellValue','buildSetGroups','buildDoorFieldGroups','buildHardwareGroups']){if(!new RegExp('export function '+fn+'\\\\b').test(h))throw new Error('Missing helper export: '+fn);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/hardware/HardwareSetConfig/hardwareConstants.ts` exists.
    - File `components/hardware/HardwareSetConfig/hardwareHelpers.ts` exists.
    - `head -1 components/hardware/HardwareSetConfig/hardwareConstants.ts` does NOT output `'use client';` (pure file).
    - `head -1 components/hardware/HardwareSetConfig/hardwareHelpers.ts` does NOT output `'use client';` (pure file).
    - grep `^export const REQUIRED_COLUMN_DEFS` in hardwareConstants.ts returns 1 match.
    - grep `^export const GROUPING_OPTIONS` in hardwareConstants.ts returns 1 match.
    - grep `^export const USAGE_OPTIONS` in hardwareConstants.ts returns 1 match.
    - grep `^export type GroupByOption` in hardwareHelpers.ts returns 1 match.
    - grep `^export interface HardwareItemUsage` in hardwareHelpers.ts returns 1 match.
    - grep `^export interface HardwareGroup` in hardwareHelpers.ts returns 1 match.
    - grep `^export function getDoorHwSetName\b` returns 1 match.
    - grep `^export function formatDoorTags\b` returns 1 match.
    - grep `^export function getItemValue\b` returns 1 match.
    - grep `^export function getUsageCellValue\b` returns 1 match.
    - grep `^export function buildSetGroups\b` returns 1 match.
    - grep `^export function buildDoorFieldGroups\b` returns 1 match.
    - grep `^export function buildHardwareGroups\b` returns 1 match.
    - grep `from '../../../types'` in hardwareHelpers.ts returns at least 1 match (depth-correct import of Door/HardwareSet/HardwareItem).
    - `npx tsc --noEmit 2>&1 | grep -E "HardwareSetConfig/(hardwareConstants|hardwareHelpers)"` returns no new TS2305/TS2307/TS2306 errors.
  </acceptance_criteria>
  <done>Pure constants and helpers files created with no `'use client'` directive, named exports for all 3 constants and 7 helper functions plus 3 local types, correct depth-adjusted import of project types.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create HardwareGroupTable.tsx sub-component ('use client' required)</name>
  <files>
    components/hardware/HardwareSetConfig/HardwareGroupTable.tsx
  </files>
  <read_first>
    components/hardware/HardwareSetConfig.tsx (lines 273-396 — full HardwareGroupTable component body to extract; capture its exact props, body, and the helper functions it calls)
    components/hardware/HardwareSetConfig/hardwareHelpers.ts (just-created — confirm export names for helpers and types: formatDoorTags, getUsageCellValue, getItemValue, HardwareGroup, HardwareItemUsage)
    components/hardware/HardwareSetConfig/hardwareConstants.ts (just-created — confirm REQUIRED_COLUMN_DEFS export)
    .claude/skills/code-standards/SKILL.md (component naming, named exports, props interface)
  </read_first>
  <action>
    Create `components/hardware/HardwareSetConfig/HardwareGroupTable.tsx`.

    Line 1 (LITERAL FIRST LINE, before any imports or comments — VER-02 / D-13): `'use client';`

    **Step A — Copy the HardwareGroupTable body from lines 273-396 of the flat file:**
    - The component is currently declared as `const HardwareGroupTable: React.FC<{...}> = ({...}) => {...};` or similar. Change to a named export: `export const HardwareGroupTable: React.FC<{...}> = ({...}) => {...};`
    - Keep the props interface exactly as defined inline. If it is declared as a standalone interface above the component, copy that too.
    - Keep the body bit-identical — no logic changes, no rename, no inline reflow.

    **Step B — Imports:**
    Walk the HardwareGroupTable body and identify every symbol it references that isn't local. Adjusted for the sub-directory location, imports typically include:

    ```typescript
    import React from 'react';
    // Only the lucide-react icons HardwareGroupTable actually uses — read body to confirm
    import { /* e.g. ChevronDown, ChevronRight, Eye */ } from 'lucide-react';
    // Sibling imports — same directory, NOT depth-adjusted (just './')
    import { REQUIRED_COLUMN_DEFS } from './hardwareConstants';
    import { formatDoorTags, getUsageCellValue, getItemValue, type HardwareGroup, type HardwareItemUsage } from './hardwareHelpers';
    // Project-root types — depth-adjusted from '../../types' to '../../../types'
    import type { Door, HardwareItem } from '../../../types';
    ```

    Trim to only what the body references. Confirm by reading lines 273-396:
    - If body uses only `formatDoorTags` and `getUsageCellValue` (not `getItemValue`), import only those two.
    - If body iterates `REQUIRED_COLUMN_DEFS`, import it.
    - If body references the `HardwareGroup` type as a prop, `type HardwareGroup` must be imported.

    **Step C — Strict path-depth check:**
    - `'../../types'` is WRONG inside the sub-directory. Use `'../../../types'`.
    - `'./hardwareHelpers'` is CORRECT (siblings in the same sub-directory).
    - `'./hardwareConstants'` is CORRECT.
    - Alias paths `@/...` are UNCHANGED.

    **Formatting:** Use **2-space indent** per .claude/skills/code-standards/SKILL.md (Prettier-enforced project standard). Re-indent the copied HardwareGroupTable body from 4-space to 2-space. Accept the cosmetic diff against the source file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('components/hardware/HardwareSetConfig/HardwareGroupTable.tsx','utf8');if(t.split(/\r?\n/)[0]!==\"'use client';\")throw new Error('Line 1 must be \\'use client\\';');if(!/export const HardwareGroupTable/.test(t))throw new Error('HardwareGroupTable not exported');if(!/from ['\"]\.\/hardwareHelpers['\"]/.test(t))throw new Error('Must import from ./hardwareHelpers');if(/from ['\"]\.\.\/\.\.\/types\b/.test(t))throw new Error('Wrong depth: ../../types should be ../../../types');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/hardware/HardwareSetConfig/HardwareGroupTable.tsx` exists.
    - `head -1 components/hardware/HardwareSetConfig/HardwareGroupTable.tsx` outputs exactly: `'use client';`
    - grep `^export const HardwareGroupTable` returns 1 match.
    - grep `from '\./hardwareHelpers'` returns at least 1 match (sibling import — formatDoorTags / getUsageCellValue / getItemValue / HardwareGroup / HardwareItemUsage live here).
    - grep `from '\./hardwareConstants'` returns at least 1 match IF the body references `REQUIRED_COLUMN_DEFS` (otherwise OK to be 0).
    - grep `from '\.\./\.\./types'` returns 0 matches (would be wrong depth).
    - grep `from '\.\./\.\./\.\./types'` returns at least 1 match IF the body references project-root types.
    - `npx tsc --noEmit 2>&1 | grep -E "HardwareSetConfig/HardwareGroupTable"` returns no new TS2305/TS2307/TS2306 errors.
  </acceptance_criteria>
  <done>HardwareGroupTable sub-component file exists with `'use client';` as literal line 1, named const export, correct sibling and depth-adjusted imports, and bit-identical body extracted from lines 273-396 of the flat file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create barrel index.tsx (main component + inline handler + JSX), delete flat file, verify tsc clean</name>
  <files>
    components/hardware/HardwareSetConfig/index.tsx
    components/hardware/HardwareSetConfig.tsx
  </files>
  <read_first>
    components/hardware/HardwareSetConfig.tsx (full file — copy imports, the export type re-export at line 15, the HardwareSetConfigProps interface at lines 19-25, the HardwareSetConfig main component body and JSX at lines 400-853; INCLUDING the inline download handler per D-07)
    components/hardware/HardwareSetConfig/hardwareConstants.ts (confirm exports)
    components/hardware/HardwareSetConfig/hardwareHelpers.ts (confirm exports)
    components/hardware/HardwareSetConfig/HardwareGroupTable.tsx (confirm export)
    .planning/tsc-baseline.txt (baseline for VER-01 diff)
  </read_first>
  <action>
    Create `components/hardware/HardwareSetConfig/index.tsx`.

    Line 1 (LITERAL FIRST LINE, before any imports or comments — VER-02 / D-13): `'use client';`

    **Step A — Header / imports:**
    Copy the import list from lines 1-10 of the flat file, then adjust:
    - `from '../../types'` → `from '../../../types'`
    - `from '../../types/hardwareSetTypes'` → `from '../../../types/hardwareSetTypes'`
    - `@/services/excelTheme`, `@/services/pdfTheme` — UNCHANGED (alias paths)
    - `@/components/ui/CollapseAllButton` — UNCHANGED
    - Add sibling imports for the new sub-files:
      ```typescript
      import { REQUIRED_COLUMN_DEFS, GROUPING_OPTIONS, USAGE_OPTIONS } from './hardwareConstants';
      import {
          getDoorHwSetName,
          formatDoorTags,
          getItemValue,
          getUsageCellValue,
          buildSetGroups,
          buildDoorFieldGroups,
          buildHardwareGroups,
          type GroupByOption,
          type HardwareItemUsage,
          type HardwareGroup,
      } from './hardwareHelpers';
      import { HardwareGroupTable } from './HardwareGroupTable';
      ```
    - REMOVE the inline declarations of `REQUIRED_COLUMN_DEFS`, `GROUPING_OPTIONS`, `USAGE_OPTIONS` (now imported from hardwareConstants).
    - REMOVE the inline declarations of the 7 helper functions (now imported from hardwareHelpers).
    - REMOVE the inline `GroupByOption` type, `HardwareItemUsage` interface, `HardwareGroup` interface (now imported as types from hardwareHelpers).
    - REMOVE the inline `HardwareGroupTable` component definition (now imported).

    Walk through and trim only imports that index.tsx actually uses. For example, if `HardwareGroupTable` is the only consumer of `getItemValue`, you don't need to import `getItemValue` in index.tsx — only HardwareGroupTable needs it. Read the main component body (lines 400-853) and import only what THAT body references.

    **Step B — Type re-export (D-08 / preserves consumer-facing named type):**
    Immediately after imports:
    ```typescript
    // ─── Exported types (kept for downstream services) ───────────────────────────

    export type { HardwareSetExportConfig } from '../../../types/hardwareSetTypes';
    ```

    **Step C — Local types:**
    Copy the `HardwareSetConfigProps` interface and the inline `ExportFormat` type as they appear in the flat file (lines 19-28). The `GroupByOption`, `HardwareItemUsage`, `HardwareGroup` types are now imported — do NOT redeclare.

    ```typescript
    interface HardwareSetConfigProps {
        doors: Door[];
        hardwareSets: HardwareSet[];
        projectName: string;
        onBack: () => void;
        onExport?: (config: HardwareSetExportConfig) => void;
    }

    type ExportFormat = 'xlsx' | 'pdf';
    ```

    **Step D — Main component (lines 400-853):**
    Copy the entire `const HardwareSetConfig: React.FC<HardwareSetConfigProps> = ({...}) => {...};` block — INCLUDING the inline handleDownload (D-07 says it stays inline; do NOT extract to a hook).

    Inside the main component body, replace any reference to:
    - `REQUIRED_COLUMN_DEFS` (now imported)
    - `GROUPING_OPTIONS` (now imported)
    - `USAGE_OPTIONS` (now imported)
    - `getDoorHwSetName`, `formatDoorTags`, `getItemValue`, `getUsageCellValue`, `buildSetGroups`, `buildDoorFieldGroups`, `buildHardwareGroups` (all imported)
    - `<HardwareGroupTable ...>` JSX (now imported)

    These remain referenced by name in the body — only their declarations were removed. The body is otherwise unchanged.

    **Step E — Default export (with SKILL.md §3 deviation note):**
    At the end of the file, emit the default export with an explanatory comment so the deliberate deviation from SKILL.md §3 (which forbids default exports for components) is documented inline:
    ```typescript
    // Default export preserved for consumer backward-compatibility — deliberate deviation from
    // .claude/skills/code-standards/SKILL.md §3 named-export rule, required for zero-impact
    // structural refactor (all 4 consumers default-import this component path).
    export default HardwareSetConfig;
    ```

    **Step F — Final structural verification:**
    - Line 1 is `'use client';` (no leading whitespace, no comment above).
    - `export type { HardwareSetExportConfig } from '../../../types/hardwareSetTypes';` present (D-08).
    - `export default HardwareSetConfig;` present (consumers use default import).
    - All three sibling imports (`./hardwareConstants`, `./hardwareHelpers`, `./HardwareGroupTable`) present.
    - No inline declarations of the extracted constants, helpers, types, or sub-component remain.
    - Inline handleDownload body still present in the main component (per D-07 — NOT extracted to a hook).

    **Step G — Delete the flat file:**
    Only after `index.tsx` is fully written:
    - Run: `rm components/hardware/HardwareSetConfig.tsx`
    - PowerShell: `Remove-Item components/hardware/HardwareSetConfig.tsx`

    **Step H — VER-01 tsc verification:**
    ```bash
    npx tsc --noEmit 2>&1 | grep -E "TS2305|TS2307|TS2306" > /tmp/post-split-hardwareset.txt
    diff .planning/tsc-baseline.txt /tmp/post-split-hardwareset.txt
    ```
    No lines added (`>` markers) in the diff. Lines removed (`<` markers) are acceptable. If new errors appear, fix imports/types in the new sub-files and re-run until clean.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const idx=fs.readFileSync('components/hardware/HardwareSetConfig/index.tsx','utf8');if(idx.split(/\r?\n/)[0]!==\"'use client';\")throw new Error('index.tsx line 1 must be \\'use client\\';');if(!/export default HardwareSetConfig/.test(idx))throw new Error('index.tsx must export default HardwareSetConfig');if(!/export type \{ HardwareSetExportConfig \} from ['\"]\.\.\/\.\.\/\.\.\/types\/hardwareSetTypes['\"]/.test(idx))throw new Error('Type re-export missing or wrong path');if(!/from ['\"]\.\/hardwareConstants['\"]/.test(idx))throw new Error('hardwareConstants import missing');if(!/from ['\"]\.\/hardwareHelpers['\"]/.test(idx))throw new Error('hardwareHelpers import missing');if(!/from ['\"]\.\/HardwareGroupTable['\"]/.test(idx))throw new Error('HardwareGroupTable import missing');if(/const REQUIRED_COLUMN_DEFS\s*=/.test(idx))throw new Error('REQUIRED_COLUMN_DEFS still inline — must be imported');if(/function getDoorHwSetName\b/.test(idx))throw new Error('getDoorHwSetName still inline — must be imported');if(fs.existsSync('components/hardware/HardwareSetConfig.tsx'))throw new Error('Flat file components/hardware/HardwareSetConfig.tsx must be DELETED');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/hardware/HardwareSetConfig/index.tsx` exists.
    - `head -1 components/hardware/HardwareSetConfig/index.tsx` outputs exactly: `'use client';`
    - grep `^export default HardwareSetConfig` in index.tsx returns 1 match.
    - grep `export type \{ HardwareSetExportConfig \} from '../../../types/hardwareSetTypes'` in index.tsx returns 1 match (D-08).
    - grep `from '\./hardwareConstants'` in index.tsx returns 1 match.
    - grep `from '\./hardwareHelpers'` in index.tsx returns 1 match.
    - grep `from '\./HardwareGroupTable'` in index.tsx returns 1 match.
    - grep `^const REQUIRED_COLUMN_DEFS\s*=` in index.tsx returns 0 matches (extracted — must NOT be inline).
    - grep `^function getDoorHwSetName\b` in index.tsx returns 0 matches (extracted).
    - grep `^const HardwareGroupTable: React\.FC` in index.tsx returns 0 matches (extracted).
    - grep `const handleDownload\s*=` in index.tsx returns at least 1 match (inline per D-07 — must NOT be extracted to a hook).
    - File `components/hardware/HardwareSetConfig.tsx` does NOT exist (deleted).
    - Command `npx tsc --noEmit 2>&1 | grep -E "TS2305|TS2307|TS2306" > /tmp/post-split-hardwareset.txt; diff .planning/tsc-baseline.txt /tmp/post-split-hardwareset.txt` shows NO lines added (no `>` markers in diff output) — VER-01.
    - `npx tsc --noEmit 2>&1 | grep -E "hardware/HardwareSetConfig"` returns zero TS2305/TS2307/TS2306 errors.
  </acceptance_criteria>
  <done>Sub-directory module fully assembled: barrel index.tsx contains 'use client' as line 1, explicit default export, type re-export, three sibling imports, and inline handleDownload (per D-07 — NOT extracted); flat file deleted; tsc diff against baseline shows zero new TS2305/TS2307/TS2306 errors.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Directory structure:**
   ```
   components/hardware/
   ├── HardwareSetConfig/
   │   ├── index.tsx
   │   ├── hardwareConstants.ts
   │   ├── hardwareHelpers.ts
   │   └── HardwareGroupTable.tsx
   ├── ElectrificationEditor.tsx   (untouched)
   ├── FinishSystemEditor.tsx       (untouched)
   ├── HardwarePrepEditor.tsx       (untouched)
   ├── HardwareSetModal.tsx          (untouched)
   ├── HardwareTrashModal.tsx        (untouched)
   ├── HingeSpecEditor.tsx           (untouched)
   ├── HardwareScheduleView.tsx      (untouched)
   ├── HardwareSetExpandedRow.tsx    (untouched)
   └── HardwareSetsManager.tsx       (untouched)
   ```
   No `HardwareSetConfig.tsx` flat file present.

2. **VER-02 ('use client' first line — only on files using hooks/browser APIs):**
   ```bash
   head -1 components/hardware/HardwareSetConfig/index.tsx            # MUST be: 'use client';
   head -1 components/hardware/HardwareSetConfig/HardwareGroupTable.tsx  # MUST be: 'use client';
   head -1 components/hardware/HardwareSetConfig/hardwareConstants.ts    # MUST NOT be 'use client'; (pure)
   head -1 components/hardware/HardwareSetConfig/hardwareHelpers.ts     # MUST NOT be 'use client'; (pure)
   ```

3. **VER-03 (explicit default re-export):**
   `grep "export default HardwareSetConfig" components/hardware/HardwareSetConfig/index.tsx` returns 1 match.

4. **VER-01 (tsc diff zero new errors):**
   ```bash
   npx tsc --noEmit 2>&1 | grep -E "TS2305|TS2307|TS2306" > /tmp/post.txt
   diff .planning/tsc-baseline.txt /tmp/post.txt
   ```
   No `>` lines (additions) in output.

5. **Consumer compatibility:**
   - `app/project/[id]/reports/hardware-set/page.tsx:17` — `dynamic(() => import('@/components/hardware/HardwareSetConfig'))` resolves to new index.tsx.
   - `components/reports/ReportGenerationCenter.tsx:5` — `import HardwareSetConfig from '../hardware/HardwareSetConfig';` resolves to new index.tsx default export.
   - `views/ReportsView.tsx:7` — same; default import resolves.
   No consumer file is modified.

6. **D-07 verified — inline handler preserved:**
   `grep -n "const handleDownload" components/hardware/HardwareSetConfig/index.tsx` returns at least 1 match (the download handler remained inline, NOT extracted to a hook).
</verification>

<success_criteria>
- COMP-03: ✓ `components/hardware/HardwareSetConfig/index.tsx` exists as barrel; flat file deleted; sub-files cover all three identified domains (hardware preview table UI, constants, helpers — per CONTEXT D-06).
- COMP-04: ✓ All 4 consumer imports resolve unchanged; tsc --noEmit diff vs baseline shows zero new TS2305/TS2307/TS2306 errors.
- VER-02: ✓ index.tsx and HardwareGroupTable.tsx have `'use client'` as literal line 1; hardwareConstants.ts and hardwareHelpers.ts correctly do NOT (pure data/functions).
- VER-03: ✓ index.tsx contains explicit default export (`export default HardwareSetConfig`).
- D-07: ✓ handleDownload remains inline in index.tsx — NOT extracted to a hook.
- D-08: ✓ `export type { HardwareSetExportConfig } from '../../../types/hardwareSetTypes'` preserved in index.tsx.
- index.tsx target ~454 lines (per D-07, "may slightly exceed 300 lines" is acceptable for this component).
- Phase 8 Plan 03 (verification gate) can now run.
</success_criteria>

<output>
After completion, create `.planning/phases/08-component-config-splits/08-02-SUMMARY.md` with:
- `one_liner` frontmatter field
- Files created (4) and deleted (1)
- Final line counts for each new file
- Confirmation that `'use client'` is line 1 in exactly index.tsx and HardwareGroupTable.tsx (NOT in hardwareConstants.ts or hardwareHelpers.ts)
- Confirmation that `export default HardwareSetConfig` is present in index.tsx (VER-03)
- Confirmation that handleDownload remained INLINE per D-07 (a grep + line range to prove it)
- Confirmation that tsc --noEmit diff vs baseline produced zero new TS2305/TS2307/TS2306 lines
</output>

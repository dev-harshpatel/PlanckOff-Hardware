---
phase: 12-re-render-fetch-audit
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - app/project/[id]/reports/pricing/page.tsx
  - .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md
autonomous: false
requirements: [PERF-02]
must_haves:
  truths:
    - "app/project/[id]/reports/pricing/page.tsx useEffect dep array includes both id and addToast (was only [id])"
    - "12-VERIFICATION.md exists and documents pass/fail status for PERF-01, PERF-02, PERF-03 against quantitative grep+tsc evidence from the modified files"
    - "tsc --noEmit error count after all three plans completes is equal to (not greater than) the baseline of 142 lines captured at .planning/tsc-baseline.txt"
    - "The two intentional eslint-disable suppressions on useProjectData.ts (lines 86, 232), useProjectPersistence.ts (line 229), and useProjectUploads.ts (line 161) remain in place and untouched — verified by line-number grep"
    - "Human verifies the application loads, project list renders, create/restore project work without console errors, and a door table cell edit auto-saves without a re-fetch occurring"
  artifacts:
    - path: "app/project/[id]/reports/pricing/page.tsx"
      provides: "Pricing report page with corrected useEffect dependency array"
      contains: "addToast"
      min_lines: 100
    - path: ".planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md"
      provides: "VER gate result document — PERF-01/02/03 pass/fail with evidence"
      min_lines: 60
  key_links:
    - from: "app/project/[id]/reports/pricing/page.tsx useEffect at line 35-80"
      to: "deps array [id, addToast]"
      via: "useEffect dep array suffix"
      pattern: "\\}, \\[id, addToast\\]\\);"
    - from: ".planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md"
      to: "evidence for each PERF requirement"
      via: "grep counts and tsc diff cited in document"
      pattern: "PERF-0[123].*PASS"
---

<objective>
Close the only remaining concrete PERF-02 gap surfaced by the research (the pricing page `useEffect` missing `addToast` in its dep array), then write the phase-12 verification gate document and run the human smoke test that confirms zero behavior regression from Plans 12-01 and 12-02.

Purpose: PERF-02 has two parts in the research — (a) the missing-dep fix on `pricing/page.tsx:80`, which is a 1-line edit and the only concrete issue with a low-risk fix; (b) the pricing page double-fetch issue, which the research explicitly defers ("requires a `subscribeOnly` option to `useProjectData` — out of scope for this phase per Pitfall 4 / Open Question 3"). This plan implements (a) and records (b) as documented out-of-scope in the verification doc. Combined with Plans 01 (ProjectContext) and 02 (ProjectView), this closes PERF-01 / PERF-02 / PERF-03 for the audit scope identified in research.

Output: 1-line dep array fix on `pricing/page.tsx`, a thorough `12-VERIFICATION.md` documenting PERF-01/02/03 evidence and out-of-scope items, and a human checkpoint confirming the app runs identically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-re-render-fetch-audit/12-RESEARCH.md
@.planning/tsc-baseline.txt
@app/project/[id]/reports/pricing/page.tsx
@.planning/phases/12-re-render-fetch-audit/12-01-PLAN.md
@.planning/phases/12-re-render-fetch-audit/12-02-PLAN.md

<interfaces>
<!-- addToast is destructured from useToast() at line 28 of pricing/page.tsx — it is a stable reference (research §Pattern 4 "addToast identity is stable") so adding it to the dep array is safe. -->

```typescript
// app/project/[id]/reports/pricing/page.tsx current shape:
const { addToast } = useToast();  // line 28 — stable reference per Phase 6 Realtime wiring

useEffect(() => {
  if (!id) return;
  async function load() { /* uses addToast inside the catch block at line 73 */ }
  load();
}, [id]);  // line 80 — addToast missing
```

ERRORS.GENERAL.UNEXPECTED imported from `@/constants/errors` (already present).
</interfaces>

<intentional_eslint_suppressions>
<!-- Pricing/page.tsx has NO eslint-disable. The dep array fix is genuine — adding addToast satisfies react-hooks/exhaustive-deps without a suppression. -->
<!-- DO NOT TOUCH these files in this plan (they belong to other concerns): -->
<!--   hooks/useProjectData.ts line 86 - polling deps suppression -->
<!--   hooks/useProjectData.ts line 232 - loadProjectData deps suppression -->
<!--   hooks/useProjectPersistence.ts line 229 - auto-save deps suppression -->
<!--   hooks/useProjectUploads.ts line 161 - mount-only effect suppression -->
</intentional_eslint_suppressions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add addToast to pricing page useEffect dep array</name>
  <files>app/project/[id]/reports/pricing/page.tsx</files>
  <read_first>
    - app/project/[id]/reports/pricing/page.tsx (must read lines 1-109 verbatim — pay attention to line 28 where addToast is destructured and line 80 where the deps array is `[id]`)
    - .planning/phases/12-re-render-fetch-audit/12-RESEARCH.md (§Code Examples "Correct dependency array for pricing page useEffect" and Pattern 4 "Stabilizing useEffect Dependency Arrays")
  </read_first>
  <behavior>
    - The useEffect at lines 35-80 currently has deps `[id]` (line 80) — change to `[id, addToast]`
    - addToast is used inside the catch block at line 73; the lint rule react-hooks/exhaustive-deps wants it in the deps
    - Behavior is byte-identical: addToast identity is stable from useToast() (per Phase 6 ref-setter pattern), so adding it to deps does NOT cause the effect to re-run
  </behavior>
  <action>
    Edit `app/project/[id]/reports/pricing/page.tsx`:

    Replace line 80 exactly:
    ```typescript
      }, [id]);
    ```
    with:
    ```typescript
      }, [id, addToast]);
    ```

    Do NOT modify any other line. Do NOT touch the useProjectData call at line 29-33 (that is the out-of-scope double-fetch — see Open Question 3 in research). Do NOT touch the load() function body — the existing logic is correct.
  </action>
  <verify>
    <automated>grep -c "}, \[id, addToast\]);" "app/project/[id]/reports/pricing/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "}, \[id, addToast\]);" "app/project/[id]/reports/pricing/page.tsx" returns exactly 1
    - grep -c "}, \[id\]);" "app/project/[id]/reports/pricing/page.tsx" returns exactly 0 (the old form is fully replaced)
    - grep -c "const { addToast } = useToast();" "app/project/[id]/reports/pricing/page.tsx" returns exactly 1 (destructure preserved)
    - npx tsc --noEmit 2>&1 | wc -l shows no increase above .planning/tsc-baseline.txt count (142)
    - Total file line count of pricing/page.tsx is 109 (unchanged from pre-edit)
  </acceptance_criteria>
  <done>Pricing page useEffect dep array correctly lists [id, addToast]. No other behavior change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 12-VERIFICATION.md documenting PERF-01/02/03 evidence and out-of-scope items</name>
  <files>.planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-re-render-fetch-audit/12-RESEARCH.md (the entire document — needed to cite specific lines verified)
    - .planning/phases/12-re-render-fetch-audit/12-01-PLAN.md (acceptance criteria — replicated as evidence checks)
    - .planning/phases/12-re-render-fetch-audit/12-02-PLAN.md (acceptance criteria — replicated as evidence checks)
    - .planning/tsc-baseline.txt (the 142-line baseline that must be preserved)
    - contexts/ProjectContext.tsx (post-Plan-01 state — to grep current useCallback / useMemo counts)
    - views/ProjectView.tsx (post-Plan-02 state — to grep current useCallback / useMemo counts)
    - app/project/[id]/reports/pricing/page.tsx (post-Task-1 state — to confirm dep array)
  </read_first>
  <behavior>
    - The document declares PASS or FAIL for each of PERF-01, PERF-02, PERF-03 with grep-verified evidence
    - The document lists out-of-scope items deferred (pricing page double-fetch via subscribeOnly hook option; ProjectContext structural split into ProjectListContext + ProjectActionsContext)
    - The document lists the 4 intentional eslint-disable suppressions verified untouched (line-number grep results)
    - The document includes the exact tsc --noEmit output line count and confirms it equals baseline 142
  </behavior>
  <action>
    Create `.planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md` with the following structure (fill the placeholders with actual grep/command output):

    ```markdown
    # Phase 12: Re-render & Fetch Audit — Verification

    **Verified:** YYYY-MM-DD
    **Phase:** 12-re-render-fetch-audit
    **Plans included:** 12-01 (ProjectContext), 12-02 (ProjectView), 12-03 (pricing page + this gate)
    **Status:** {OVERALL_PASS_OR_FAIL}

    ---

    ## Baseline

    `.planning/tsc-baseline.txt` line count: **142**

    Post-phase `npx tsc --noEmit 2>&1 | wc -l`: **{ACTUAL_COUNT}**
    Diff: **{ACTUAL_COUNT - 142}** (must be 0 for PASS)

    ---

    ## PERF-01 — Stable values and callbacks on data-heavy pages

    **Requirement:** Stable values and callbacks on data-heavy pages (DoorScheduleManager, HardwareSetsManager, PricingReportConfig, ProjectView) are wrapped in useMemo/useCallback; no component re-renders more than once for a single user action that affects only its own state.

    **Status:** {PASS | FAIL}

    **Evidence — ProjectContext.tsx (Plan 12-01):**
    - `grep -c "useCallback" contexts/ProjectContext.tsx` → **{N}** (expected >= 11)
    - `grep -c "useMemo" contexts/ProjectContext.tsx` → **{N}** (expected >= 1)
    - `grep -E "const contextValue = useMemo" contexts/ProjectContext.tsx` → {match text}
    - All 8 action callbacks (addProject, updateProject, deleteProject, restoreProjectFn, permDeleteProject, updateInventory, overwriteInventory, saveSettings) wrapped in useCallback: {YES/NO}
    - Provider value memoized via useMemo: {YES/NO}

    **Evidence — ProjectView.tsx (Plan 12-02):**
    - `grep -c "useCallback" views/ProjectView.tsx` → **{N}** (expected >= 6)
    - `grep -c "useMemo" views/ProjectView.tsx` → **{N}** (expected >= 3)
    - `grep -c "() => setProcessingTasks(prev => prev.filter" views/ProjectView.tsx` → **{N}** (expected 0 — both inline cancel arrows replaced)

    ---

    ## PERF-02 — useEffect deps and shared fetch

    **Requirement:** No useEffect triggers a Supabase fetch with overly-broad or missing dep array; shared data fetched once; intentional suppressions documented.

    **Status:** {PASS_WITH_SCOPED_DEFERRALS | FAIL}

    **Evidence — pricing/page.tsx (Plan 12-03):**
    - `grep -c "}, \[id, addToast\]);" app/project/[id]/reports/pricing/page.tsx` → **{N}** (expected 1)
    - `grep -c "}, \[id\]);" app/project/[id]/reports/pricing/page.tsx` → **{N}** (expected 0)

    **Intentional eslint-disable suppressions verified untouched:**
    - `hooks/useProjectData.ts` line 86 — polling deps: {grep line content}
    - `hooks/useProjectData.ts` line 232 — loadProjectData deps: {grep line content}
    - `hooks/useProjectPersistence.ts` line 229 — auto-save deps: {grep line content}
    - `hooks/useProjectUploads.ts` line 161 — mount-only effect: {grep line content}

    Use: `grep -n "eslint-disable-next-line react-hooks/exhaustive-deps" hooks/useProjectData.ts hooks/useProjectPersistence.ts hooks/useProjectUploads.ts`

    **Out of scope — explicitly deferred per RESEARCH Open Question 3 / Pitfall 4:**
    - Pricing page double-fetch: the page fetches its own data AND useProjectData fires loadProjectData on mount, hitting 3 of the same endpoints twice. Resolving requires a `subscribeOnly?: boolean` option on useProjectData (signature change with cascading test surface). Deferred to a future plan once React Query migration is greenlit (per architecture skill §State Management Tier 1 target).
    - Cross-page data sharing for report pages: report pages each fetch independently because they are separate Next.js page routes with no shared cache. This is intentional (Pitfall 4) and outside this phase's scope.

    ---

    ## PERF-03 — Write operations and UI-only state changes

    **Requirement:** Write operations update local React state directly without triggering a full dataset re-fetch; UI-only state changes do not cause Supabase read calls.

    **Status:** {PASS | FAIL}

    **Evidence — ProjectContext.tsx (Plan 12-01):**
    - `grep -c "await fetchProjects()" contexts/ProjectContext.tsx` → **{N}** (expected 0 — both occurrences removed; only the no-await `fetchProjects()` call inside the initial-mount useEffect remains)
    - `grep -E "setProjects\(prev => \[\.\.\.prev, json\.data" contexts/ProjectContext.tsx` → {match text} (addProject optimistic append confirmed)
    - `grep -E "setProjects\(prev => \[activeProject" contexts/ProjectContext.tsx` → {match text} (restoreProjectFn optimistic prepend confirmed)

    **UI-only state changes verified not to trigger fetches:**
    Manual confirmation in Task 3 — viewMode toggle, isNotesOpen toggle, isTrashModalOpen toggle, tab switches do NOT cause Supabase GET requests (verified via DevTools Network tab in human-verify task).

    ---

    ## Out-of-scope items (documented for future phase)

    | Item | Reason | Future Phase |
    |------|--------|--------------|
    | ProjectContext structural split into ProjectListContext + ProjectActionsContext | Research §Pattern 3 scope guidance: useCallback + useMemo satisfies PERF-01 without structural split. A future split would reduce re-render blast radius further for pure-read consumers (AppShell, reports/layout, reports/page, reports/submittal-package/page). | Future PERF phase |
    | Pricing page double-fetch via `subscribeOnly?: boolean` on useProjectData | Research §Open Question 3: signature change on a heavily-used hook with cascading test surface. | Future PERF phase or React Query migration |
    | React.memo on DoorTableRow | Research §Open Question 1: requires verifying renderCell prop reference stability and DoorTableRow props interface. | Future PERF phase with React Profiler measurement |
    | React Query / TanStack Query migration | Architecture skill §State Management Tier 1 target. | Standalone milestone |

    ---

    ## Baseline preserved — eslint-disable suppressions untouched

    The 4 cross-file intentional eslint-disable lines (research §Project Constraints) were NOT modified in this phase. Line numbers may have shifted slightly only if other unrelated edits occurred (none expected). Verify with:

    ```bash
    grep -n "eslint-disable-next-line react-hooks/exhaustive-deps" hooks/useProjectData.ts hooks/useProjectPersistence.ts hooks/useProjectUploads.ts
    ```

    Expected output: 4 lines total, one per source file (useProjectData has 2).

    ---

    ## Conclusion

    {PERF-01, PERF-02 (with documented scope deferrals), and PERF-03 all PASS with quantitative grep + tsc evidence. Phase 12 complete.}
    ```

    Replace ALL `{placeholders}` with actual command output. Run each grep / tsc command and paste the exact output.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md && grep -c "PERF-0[123]" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md` exists with at least 60 lines
    - grep -c "PERF-01" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 2
    - grep -c "PERF-02" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 2
    - grep -c "PERF-03" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 2
    - grep -E "PERF-01.*(PASS|FAIL)" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 1
    - grep -E "PERF-02.*(PASS|FAIL)" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 1
    - grep -E "PERF-03.*(PASS|FAIL)" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 1
    - grep -c "eslint-disable" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 4 (one citation per file)
    - The document contains an "Out of scope" section with at least 3 deferred items (ProjectContext split, pricing double-fetch, React.memo on DoorTableRow)
    - All `{placeholders}` have been replaced with actual command output — no literal `{N}` or `{YES/NO}` text remains
  </acceptance_criteria>
  <done>12-VERIFICATION.md exists with quantitative PASS/FAIL evidence for PERF-01/02/03 backed by grep counts, tsc line counts, and documented out-of-scope items.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke test — confirm no behavior regression on key flows</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md (so the user sees the automated evidence first)
    - .planning/phases/12-re-render-fetch-audit/12-RESEARCH.md (Pitfalls 1, 2, 3, 5 — the human verifier should be aware of the regression surfaces)
  </read_first>
  <action>
    Present the how-to-verify steps below to the user. Start the dev server in a background shell with `npm run dev`, wait for the user to exercise the 6 flows manually, then wait for the user's resume-signal reply. After the user replies "approved" or describes failures, append a "Human-verify outcome" section to `.planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md` containing:
    - Date of verification
    - Outcome (approved / failed)
    - If failed: which flow letter, expected vs actual, and any console error text the user pasted
    - If approved: a single line "All 6 flows PASS — verified by user {userEmail} on {date}"
  </action>
  <what-built>
    Plan 12-01 made all ProjectContext callbacks stable and removed re-fetch after create/restore. Plan 12-02 stabilized all ProjectView callbacks. Plan 12-03 fixed the pricing page useEffect dep array and wrote the verification gate document. Zero behavior change was intended — every visible UI flow should work identically to pre-phase-12 behavior.
  </what-built>
  <how-to-verify>
    Start the dev server and exercise these 6 flows. After each, confirm the listed result.

    Prereq:
    1. `npm run dev` — server starts on port 3000 without errors in the terminal
    2. Open `http://localhost:3000` and sign in to an account with at least 2 existing projects

    **Flow A — Create project (PERF-03 critical):**
    1. On the dashboard, click "+ New Project"
    2. Fill in name + client + location and submit
    3. Open DevTools Network tab BEFORE submitting; filter by "projects"
    4. Confirm: exactly ONE `POST /api/projects` fires, and NO follow-up `GET /api/projects` request fires (was firing GET before this phase)
    5. Confirm: the new project appears at the END of the project grid (optimistic append) without a full reload of the list

    **Flow B — Restore project (PERF-03 critical):**
    1. Navigate to Trash modal (delete a project first if no trash exists)
    2. Click Restore on a trashed project
    3. Open DevTools Network tab; filter by "projects"
    4. Confirm: exactly ONE `DELETE /api/projects/{id}?restore=true` fires, and NO follow-up `GET /api/projects` (was firing GET before)
    5. Confirm: the restored project appears at the TOP of the active project grid (optimistic prepend)
    6. Confirm: the trashed project is removed from the trash modal

    **Flow C — View modes do not fetch (PERF-03):**
    1. Open any project (ProjectView)
    2. Open DevTools Network tab; clear and filter by "api/projects"
    3. Click the "Hardware" → "Split" → "Doors" view mode buttons in sequence
    4. Confirm: ZERO new `/api/projects` requests fire during these toggles
    5. Click Notes button (open and close)
    6. Confirm: still ZERO new `/api/projects` requests

    **Flow D — Door table cell edit auto-save (PERF-01 + persistence regression):**
    1. In a project with doors, click into a door's "Width" cell and change the value
    2. Confirm: within 1 second a save toast appears OR the save indicator goes through saving → saved (no immediate save on load — isInitialMount guard preserved per Pitfall 1)
    3. Refresh the page — the change persists

    **Flow E — Realtime echo does not duplicate (PERF-03 + Pitfall 5):**
    1. Open the dashboard in TWO browser tabs side by side
    2. In tab 1, create a new project
    3. Confirm: in tab 2 the new project appears within ~2 seconds, and there is NO duplicate (the realtime INSERT handler short-circuits when the optimistic append already added the project — only the second-tab subscriber should add it)
    4. In tab 1, the project should appear exactly ONCE

    **Flow F — Pricing report still loads (PERF-02 + general smoke):**
    1. Open any project with hardware + doors
    2. Navigate to Reports → Pricing Report
    3. Confirm: the report renders within ~2 seconds, shows door count and set count, and the proposal/items/extra-expenses tabs all work
    4. Confirm: no console errors in DevTools console

    **If any flow fails:** describe the failure (which flow, what was expected, what actually happened, any console errors).
  </how-to-verify>
  <verify>
    <automated>grep -c "Human-verify outcome" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md</automated>
  </verify>
  <resume-signal>Reply with one of:
    - "approved" — all 6 flows PASS; mark Phase 12 complete
    - "fail: {flow letter} {description}" — describe the failing flow; the planner will run `/gsd:plan-phase 12 --gaps` to create gap-closure plans
  </resume-signal>
  <acceptance_criteria>
    - User has explicitly typed "approved" or has described specific failures
    - If approved: Phase 12 can transition to complete in ROADMAP.md and STATE.md
    - If failures reported: those failures are recorded in 12-VERIFICATION.md "Failed flows" section before gap-closure planning begins
    - grep -c "Human-verify outcome" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md returns >= 1 after the checkpoint completes (section was appended)
  </acceptance_criteria>
  <done>Human has confirmed all 6 flows pass OR has reported specific failures that can be turned into gap-closure tasks. The verification document is updated to reflect the human-verify outcome.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
# 1. Pricing page dep fix present
grep -c "}, \[id, addToast\]);" "app/project/[id]/reports/pricing/page.tsx"     # must be 1

# 2. Verification document written with PASS/FAIL for all three requirements
grep -E "PERF-0[123].*(PASS|FAIL)" .planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md | wc -l   # must be >= 3

# 3. Intentional eslint-disable lines preserved
grep -n "eslint-disable-next-line react-hooks/exhaustive-deps" hooks/useProjectData.ts hooks/useProjectPersistence.ts hooks/useProjectUploads.ts | wc -l   # must be 4

# 4. tsc baseline preserved
npx tsc --noEmit 2>&1 | wc -l   # must equal 142 (or lower if some baseline errors got fixed incidentally)

# 5. Human smoke test approved
# Manual — user replies "approved" in Task 3 checkpoint
```
</verification>

<success_criteria>
- [ ] `app/project/[id]/reports/pricing/page.tsx` has `}, [id, addToast]);` exactly once
- [ ] `12-VERIFICATION.md` exists with PASS/FAIL for PERF-01/02/03 and ≥3 out-of-scope items
- [ ] All 4 intentional eslint-disable lines verified present in useProjectData.ts (2), useProjectPersistence.ts (1), useProjectUploads.ts (1)
- [ ] `npx tsc --noEmit` output line count equals 142 (baseline) or lower
- [ ] User has typed "approved" after running the 6 smoke-test flows OR has reported specific failures that will trigger `/gsd:plan-phase 12 --gaps`
</success_criteria>

<output>
After completion, create `.planning/phases/12-re-render-fetch-audit/12-03-SUMMARY.md` containing:
- Files modified (`app/project/[id]/reports/pricing/page.tsx`, `.planning/phases/12-re-render-fetch-audit/12-VERIFICATION.md`)
- The 1-line dep array change (pricing page)
- Reference to 12-VERIFICATION.md for the full PERF-01/02/03 gate result
- Human-verify outcome (approved or list of failed flows)
- Any deviations from the plan
- Recommendation to mark Phase 12 complete in ROADMAP.md and STATE.md (only if approved)
</output>

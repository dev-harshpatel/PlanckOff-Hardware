---
phase: 07-pre-split-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/tsc-baseline.txt
autonomous: true
requirements:
  - PRE-04

must_haves:
  truths:
    - "A file .planning/tsc-baseline.txt exists containing the full tsc --noEmit output captured before any source file is modified"
    - "The baseline is stable: re-running tsc --noEmit produces the same error set (no flapping)"
  artifacts:
    - path: ".planning/tsc-baseline.txt"
      provides: "Pre-modification TypeScript error snapshot used as diff target for all subsequent plans in Phases 7-11"
      min_lines: 1
  key_links:
    - from: ".planning/tsc-baseline.txt"
      to: "all subsequent Phase 7-11 PLAN.md files"
      via: "diff comparison: npx tsc --noEmit 2>&1 | Select-String TS2305|TS2307|TS2306"
      pattern: "tsc-baseline\\.txt"
---

<objective>
Capture the TypeScript compiler error baseline before any file in this phase is touched.

Purpose: The project has 142 pre-existing tsc errors (all in utils/). Every subsequent plan modifies source files. Without a baseline, there is no way to prove a modification introduced zero new errors vs. uncovered pre-existing ones. This plan does nothing except record the current state.

Output: `.planning/tsc-baseline.txt` — full tsc --noEmit output, captured once, immutable for the rest of v2.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture tsc --noEmit baseline to .planning/tsc-baseline.txt</name>
  <files>.planning/tsc-baseline.txt</files>

  <read_first>
    - F:\PlanckOff-Hardware\tsconfig.json — confirm isolatedModules:true and moduleResolution settings before running
    - F:\PlanckOff-Hardware\next.config.ts — confirm ignoreBuildErrors:true (so tsc errors are invisible to build; baseline must be captured manually)
  </read_first>

  <action>
Run the TypeScript compiler in no-emit mode and capture its full output to `.planning/tsc-baseline.txt`. Execute from the project root `F:\PlanckOff-Hardware\`.

**PowerShell command (primary — project is Windows/PowerShell):**
```powershell
npx tsc --noEmit 2>&1 | Out-File -FilePath .planning/tsc-baseline.txt -Encoding utf8
```

**Bash alternative (if PowerShell unavailable):**
```bash
npx tsc --noEmit 2>&1 > .planning/tsc-baseline.txt
```

Do NOT filter the output (no Select-String, no grep). Capture the raw complete output — all lines — so that diff operations in later plans can detect any new error lines regardless of error code.

Expected content: approximately 142+ error lines, all referencing files in `utils/`. The file MUST NOT be empty. If the command produces no output, tsc found zero errors — confirm by checking the exit code: a zero-error run exits 0, a run with errors exits non-zero. Record both possibilities faithfully.

After writing the file, verify line count:
```powershell
(Get-Content .planning/tsc-baseline.txt).Count
```

If count is 0, re-run and investigate — the project is known to have pre-existing errors.
  </action>

  <verify>
    <automated>
      # Verify file exists and is non-empty
      # PowerShell:
      Test-Path .planning/tsc-baseline.txt
      (Get-Content .planning/tsc-baseline.txt).Count

      # The count should be greater than 0 (known pre-existing errors exist)
      # Bash alternative:
      # test -s .planning/tsc-baseline.txt && echo "EXISTS NON-EMPTY" || echo "MISSING OR EMPTY"
    </automated>
  </verify>

  <done>
    - `.planning/tsc-baseline.txt` exists
    - File is non-empty (line count > 0)
    - File was written BEFORE any source file in this phase was modified
  </done>
</task>

</tasks>

<verification>
1. `Test-Path .planning/tsc-baseline.txt` returns `True`
2. `(Get-Content .planning/tsc-baseline.txt).Count` returns a number > 0
3. File was created before any `.ts` or `.tsx` source file in the project was touched in this phase
</verification>

<success_criteria>
- `.planning/tsc-baseline.txt` exists and is non-empty
- PRE-04 requirement satisfied: baseline snapshot captured before any file is modified
- Subsequent plans can diff against this file to prove zero new errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-pre-split-cleanup/07-01-SUMMARY.md`
</output>

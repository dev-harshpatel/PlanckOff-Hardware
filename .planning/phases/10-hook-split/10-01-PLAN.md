---
phase: 10-hook-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useDoorTableState/columnDefinitions.ts
  - hooks/useDoorTableState/filterState.tsx
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02

must_haves:
  truths:
    - "hooks/useDoorTableState/columnDefinitions.ts exists as a pure TypeScript file with NO 'use client' directive and NO React hooks — it contains only interfaces, type aliases, constants, and the formatDimension utility function"
    - "hooks/useDoorTableState/filterState.tsx exists with 'use client' as its literal first line; it exports a useFilterState hook returning all filter/sort state and the filteredAndSortedDoors derived value"
    - "columnDefinitions.ts exports: ColumnDef, CustomColumn, PersistedColumnPrefs, StatusFilter, formatDimension, ALL_AVAILABLE_COLUMNS, DOOR_SECTION_KEYS, FRAME_SECTION_KEYS, HARDWARE_SECTION_KEYS — all matching the original flat file signatures exactly"
    - "filterState.tsx returns: statusFilter, setStatusFilter, doorMaterialFilter, setDoorMaterialFilter, frameMaterialFilter, setFrameMaterialFilter, searchQuery, setSearchQuery, sortConfig, handleSort, statusCounts, uniqueDoorMaterials, uniqueFrameMaterials, filteredAndSortedDoors"
    - "The original hooks/useDoorTableState.tsx flat file is NOT deleted or modified in this plan"
    - "Both sub-files are under 300 lines"
  artifacts:
    - path: "hooks/useDoorTableState/columnDefinitions.ts"
      provides: "Column type definitions, section key Sets, ALL_AVAILABLE_COLUMNS array, formatDimension utility — all pure constants/types, no hooks"
      exports: ["ColumnDef", "CustomColumn", "PersistedColumnPrefs", "StatusFilter", "formatDimension", "ALL_AVAILABLE_COLUMNS", "DOOR_SECTION_KEYS", "FRAME_SECTION_KEYS", "HARDWARE_SECTION_KEYS"]
      max_lines: 300
    - path: "hooks/useDoorTableState/filterState.tsx"
      provides: "useFilterState hook — filter/search/sort state + filteredAndSortedDoors derivation"
      exports: ["useFilterState"]
      max_lines: 300
  key_links:
    - from: "hooks/useDoorTableState/filterState.tsx"
      to: "types/index.ts"
      via: "import { Door } from '../../types'"
      pattern: "from ['\"]\\.\\.\/\\.\\.\\/types['\"]"
    - from: "hooks/useDoorTableState/filterState.tsx"
      to: "hooks/useDoorTableState/columnDefinitions.ts (indirectly via StatusFilter type)"
      via: "import { StatusFilter } from './columnDefinitions'"
      pattern: "from ['\"]\\./columnDefinitions['\"]"
---

<objective>
Extract two independent domains from `hooks/useDoorTableState.tsx` into sub-directory files:
1. `columnDefinitions.ts` — pure TypeScript constants and interfaces (lines 11–97 of source)
2. `filterState.tsx` — filter/search/sort state hook with filteredAndSortedDoors (lines 124–128, 132–133, 254–339 of source)

Purpose: Wave 1 parallel extraction. These two sub-files have no dependency on other sub-files being created in this phase. The flat file is NOT deleted — deletion happens only after the orchestrator is wired in plan 10-03.

Output: Two new files in `hooks/useDoorTableState/`. The original flat file remains untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hook-split/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hooks/useDoorTableState/columnDefinitions.ts</name>

  <read_first>
    - hooks/useDoorTableState.tsx (lines 1–97 — the entire pure-constants domain being extracted: ColumnDef interface, CustomColumn interface, PersistedColumnPrefs interface, StatusFilter type, formatDimension function, ALL_AVAILABLE_COLUMNS array, DOOR_SECTION_KEYS Set, FRAME_SECTION_KEYS Set, HARDWARE_SECTION_KEYS Set)
  </read_first>

  <files>hooks/useDoorTableState/columnDefinitions.ts</files>

  <action>
Create the directory `hooks/useDoorTableState/` (it does not yet exist) and create `hooks/useDoorTableState/columnDefinitions.ts`.

This file contains EXACTLY the pure-constants content from `hooks/useDoorTableState.tsx` lines 11–97, with these rules:

1. **NO 'use client' directive** — this file has no React hooks and no browser APIs. The first line MUST be an import or export statement. Adding 'use client' is wrong (VER-02: only required when hooks or browser APIs are present).

2. **File extension is `.ts` not `.tsx`** — no JSX in this file.

3. **Import only what is strictly needed** — the original flat file imports React and many hooks at the top; this sub-file needs NONE of those. However, check if ALL_AVAILABLE_COLUMNS or any constant references a type that requires an import. The ColumnDef interface itself defines the `type: 'text' | 'number' | 'select'` shape internally — no external type import needed.

4. **Copy verbatim** — every interface, type alias, constant, and function body must be copied exactly as they appear in the source. No logic changes. No renaming.

5. **All exports** — every item in this file must be exported (they all need to be re-exported from the barrel):
   - `export interface ColumnDef { ... }` (lines 11–19)
   - `export interface CustomColumn { ... }` (lines 21–25)
   - `export interface PersistedColumnPrefs { ... }` (lines 27–31)
   - `export type StatusFilter = 'all' | 'pending' | 'complete';` (line 33)
   - `export const formatDimension = (inches: number): string => { ... }` (lines 35–40)
   - `export const ALL_AVAILABLE_COLUMNS: ColumnDef[] = [ ... ]` (lines 42–84)
   - `export const DOOR_SECTION_KEYS = new Set([ ... ])` (lines 86–89)
   - `export const FRAME_SECTION_KEYS = new Set([ ... ])` (lines 90–94)
   - `export const HARDWARE_SECTION_KEYS = new Set([ ... ])` (lines 95–97)

6. **No additional imports** — the file starts directly with the first export statement. It has no runtime dependencies beyond what TypeScript/JavaScript provides natively.

The resulting file: first line is `export interface ColumnDef {`, approximately 90–100 lines.
  </action>

  <verify>
    <automated>
      # File exists
      Test-Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts

      # Under 300 lines
      (Get-Content F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts).Count

      # No 'use client' directive anywhere in file
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "'use client'" | Should -BeNullOrEmpty

      # All 9 required exports present
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export interface ColumnDef"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export interface CustomColumn"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export interface PersistedColumnPrefs"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export type StatusFilter"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export const formatDimension"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export const ALL_AVAILABLE_COLUMNS"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export const DOOR_SECTION_KEYS"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export const FRAME_SECTION_KEYS"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "export const HARDWARE_SECTION_KEYS"

      # No React imports (pure file)
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "^import React" | Should -BeNullOrEmpty
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/columnDefinitions.ts -Pattern "^import.*useState" | Should -BeNullOrEmpty

      # Flat file still exists (must NOT be deleted)
      Test-Path F:/PlanckOff-Hardware/hooks/useDoorTableState.tsx
    </automated>
  </verify>

  <done>
    - `hooks/useDoorTableState/columnDefinitions.ts` exists as a `.ts` file (not `.tsx`)
    - File is under 300 lines
    - First line is an export statement (NOT 'use client', NOT an import)
    - No 'use client' directive anywhere in file
    - All 9 items exported: ColumnDef, CustomColumn, PersistedColumnPrefs, StatusFilter, formatDimension, ALL_AVAILABLE_COLUMNS, DOOR_SECTION_KEYS, FRAME_SECTION_KEYS, HARDWARE_SECTION_KEYS
    - No React imports or hook references in file
    - `hooks/useDoorTableState.tsx` flat file still exists and is unmodified
  </done>
</task>

<task type="auto">
  <name>Task 2: Create hooks/useDoorTableState/filterState.tsx</name>

  <read_first>
    - hooks/useDoorTableState.tsx (read the FULL file — needed for: lines 1–10 to confirm import paths for Door type; lines 124–128 for filter state declarations; lines 132–133 for sortConfig state; lines 254–339 for statusCounts/uniqueDoorMaterials/uniqueFrameMaterials/filteredAndSortedDoors/handleSort derivations; lines 100–123 for UseDoorTableStateParams interface to determine which params filterState needs)
    - hooks/useDoorTableState/columnDefinitions.ts (just created in Task 1 — read to confirm StatusFilter is exported from there)
  </read_first>

  <files>hooks/useDoorTableState/filterState.tsx</files>

  <action>
Create `hooks/useDoorTableState/filterState.tsx` — the filter/search/sort sub-hook.

1. **'use client' is the literal first line** — before any imports, before any blank lines, before any comments. Required by VER-02 (this file calls useState and useMemo).

2. **Imports** — the sub-file is one level deeper than the flat file, so relative paths gain one `../`:
   ```typescript
   'use client';
   import { useState, useMemo } from 'react';
   import { Door } from '../../types';
   import { StatusFilter } from './columnDefinitions';
   ```
   Do NOT import `ERRORS`, `matchHardwareSet`, `migrateDoorData`, `useBackgroundUpload`, icon libraries, or UI components — those are not used by this sub-hook.

3. **Hook signature** — define a `UseFilterStateParams` interface and export `useFilterState`:
   ```typescript
   interface UseFilterStateParams {
     doors: Door[];
   }

   export function useFilterState({ doors }: UseFilterStateParams) {
     // state declarations
     // useMemo derivations
     // handleSort handler
     return { ... };
   }
   ```

4. **State to include** (copy from flat file verbatim):
   - `const [statusFilter, setStatusFilter] = useState<StatusFilter>('all');` (flat file line 124)
   - `const [doorMaterialFilter, setDoorMaterialFilter] = useState<string>('all');` (line 125)
   - `const [frameMaterialFilter, setFrameMaterialFilter] = useState<string>('all');` (line 126)
   - `const [searchQuery, setSearchQuery] = useState('');` (line 127)
   - `const [sortConfig, setSortConfig] = useState<{ key: keyof Door; direction: 'asc' | 'desc' } | null>(null);` (line 132)

5. **Derived values and handlers** (copy from flat file verbatim):
   - `statusCounts` useMemo (flat file lines 254–261)
   - `uniqueDoorMaterials` useMemo (lines 262–267)
   - `uniqueFrameMaterials` useMemo (lines 268–273)
   - `handleSort` function (lines 274–280)
   - `filteredAndSortedDoors` useMemo (lines 282–339) — this is the largest block; copy exactly

6. **Return statement** — return all 14 values:
   ```typescript
   return {
     statusFilter, setStatusFilter,
     doorMaterialFilter, setDoorMaterialFilter,
     frameMaterialFilter, setFrameMaterialFilter,
     searchQuery, setSearchQuery,
     sortConfig,
     handleSort,
     statusCounts,
     uniqueDoorMaterials,
     uniqueFrameMaterials,
     filteredAndSortedDoors,
   };
   ```

7. **Copy all function/useMemo bodies verbatim** — zero logic changes. This is a structural refactor only.

8. **No isAssigningBatch** — `isAssigningBatch` is owned by the orchestrator (it is set by action handlers in index.tsx). Do NOT include it in filterState.tsx.
  </action>

  <verify>
    <automated>
      # File exists
      Test-Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx

      # 'use client' is literal first line
      (Get-Content F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -TotalCount 1)
      # Expected: 'use client'

      # Under 300 lines
      (Get-Content F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx).Count

      # useFilterState is exported
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "export function useFilterState"

      # Depth-corrected imports
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "from '../../types'"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "from './columnDefinitions'"

      # Flat import paths NOT present (would be wrong)
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "from '\.\./types'" | Should -BeNullOrEmpty

      # Key return values present
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "filteredAndSortedDoors"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "statusCounts"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "handleSort"
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "sortConfig"

      # No orchestrator-only state leaked into this file
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "isAssigningBatch" | Should -BeNullOrEmpty
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "editingCell" | Should -BeNullOrEmpty
      Select-String -Path F:/PlanckOff-Hardware/hooks/useDoorTableState/filterState.tsx -Pattern "visibleColumns" | Should -BeNullOrEmpty

      # Flat file still exists
      Test-Path F:/PlanckOff-Hardware/hooks/useDoorTableState.tsx
    </automated>
  </verify>

  <done>
    - `hooks/useDoorTableState/filterState.tsx` exists
    - `'use client'` is the literal first line (before any import statement)
    - File is under 300 lines
    - `useFilterState` is exported as a named export
    - Imports use depth-corrected paths: `../../types` and `./columnDefinitions`
    - Returns all 14 filter/sort values: statusFilter, setStatusFilter, doorMaterialFilter, setDoorMaterialFilter, frameMaterialFilter, setFrameMaterialFilter, searchQuery, setSearchQuery, sortConfig, handleSort, statusCounts, uniqueDoorMaterials, uniqueFrameMaterials, filteredAndSortedDoors
    - No orchestrator-owned state (isAssigningBatch, editingCell, visibleColumns, etc.) leaked into this file
    - `hooks/useDoorTableState.tsx` flat file still exists and is unmodified
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `hooks/useDoorTableState/columnDefinitions.ts` exists, is `.ts` (not `.tsx`), has no 'use client', has all 9 exports
2. `hooks/useDoorTableState/filterState.tsx` exists, has 'use client' as line 1, exports useFilterState, has depth-corrected imports
3. Both files are under 300 lines
4. `hooks/useDoorTableState.tsx` flat file still exists — NOT deleted
5. Quick tsc check: `npx tsc --noEmit 2>&1 | Select-String "TS2305|TS2307|TS2306"` should show zero new errors (sub-files are not yet imported by anything)
</verification>

<success_criteria>
Both sub-files exist in `hooks/useDoorTableState/`. columnDefinitions.ts has no 'use client' and all 9 exports. filterState.tsx has 'use client' as line 1, exports useFilterState with 14 return values, and uses depth-corrected import paths. Flat file unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/10-hook-split/10-01-SUMMARY.md`
</output>
